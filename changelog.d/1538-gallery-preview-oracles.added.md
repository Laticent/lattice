- **Added: metamorphic oracles for the gallery's preview window.** Six relations that hold
  whatever bounds the previews — retention saturates, the visible set is path-independent,
  open/close is idempotent, a filter round-trip is a no-op, a recycled tile comes back painted,
  and a looks panel gives its previews back. They need no oracle, which is the point: "is 32 live
  engine documents right for an iPhone" has no answer this sandbox can compute, so a threshold
  assertion would write the answer into the test. They survived the window being re-tuned and
  then replaced outright. They run at desktop and phone widths in Chromium and on real WebKit at
  the iPhone 15 Pro profile.
