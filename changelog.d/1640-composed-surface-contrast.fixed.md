- `redline` no longer fades its own text with `opacity`. A struck `<del>` run carried `opacity: 0.85` and the `.stacked` variant's OLD card another `0.78`; CSS opacity composites the ink together with the tinted band under it, so it cost legibility on every palette and mode. Measured on the rendered deck across all 32 palettes, text runs below their threshold drop from 388 of 2496 to 184 — indaco's `.stacked` struck clause goes 3.21:1 to 5.85:1 in sampled pixels. **This is live on the PDF/PPTX export path now**, not only in the Studio and the docs Playground.
- Ten palette arms re-tuned so a curated status hue clears AA on the surface a component actually paints it on — its own 12% tint, whose background moves with the value — rather than only on the canvas: `--pass` and `--fail` on `ardesia`, `brina` and `laguna` (light), `--pass` and `--fail` on `carta`, `--fail` on `magnolia` (dark), `--pass` on `concrete` and `carbone` (dark) and on `a11y-tritanopia` (light), and the `--seq-500` ramp anchors on `onyx` and `concrete`, which `word-cloud spectrum` reads through the base's derived `--seq-700` stop. Each was solved as a TRIO rather than a single token, so no palette's pass/warn/fail separation collapses under a color-vision deficiency — eight pairs that were indistinct under protanopia are now distinct. **These reach the export path only once #1527 flips the cascade order**; they are live in the Studio and the docs Playground today.
- New gate `tools/composed-contrast.js` (+ `test/unit/palette/composed-surface-contrast.test.js`) scores the 24 surfaces components compose — an ink on a tint of itself, that band on a second own-hue card, the OKLab ramp stops the base derives from a palette anchor, and nested element `opacity` — in both cascade orders. `tools/contrast-audit.js` checks each ink against `--bg` and never against the `-bg` mix, which is why 102 palette values could go sub-threshold under #1527 with every gate green.
- New gate `test/unit/palette/cvd-trio-floor.test.js` freezes the status pairs that are distinguishable under protanopia, deuteranopia and tritanopia today, for every palette rather than only the four a11y ones. `tools/cvd-audit.js` is a report that exits 0, so a re-tune driven by any other constraint could collapse a pair with the suite green.
- `tools/check-slide-contrast.js` now reads element `opacity`. It composited the ink and its backdrop independently, which is not what a CSS group alpha does, so its number for any run inside one was optimistic — ~5:1 where the rendered pixels were 3.21:1. Validated against sampled pixels to within 0.01.
