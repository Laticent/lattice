- The deck logo sits on the atmosphere plane, not chrome. Its *kind* is chrome — frame furniture rather than author content — but a plane decides what may cover what, and a deck mark must never cover the words. On the chrome plane an off-corner `logo-style: brand` plate (a documented placement) painted over a pull-quote and erased four words.
- Status stamps return to `z-index: 100` and the authoring tabs rise to `90`. An earlier cut of the scale ran them at 50 and 40 for tidiness, which put both inside the range decks reach for by habit — a single `<div style="position:absolute;inset:0;z-index:60">` erased a CONFIDENTIAL marking from a delivered PDF. The distance from chrome is a guarantee, not spare room.
