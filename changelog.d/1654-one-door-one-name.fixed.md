- **Fixed: the Studio's add-slide gallery has one name on every launcher.** All
  five controls that open it already opened the *same* gallery, but they called it
  five different things — the preview rail said "Add slide", the editor header and
  the mobile drawer said "Insert component", the command palette said "Insert a
  component…", the Compose divider said "Insert slide below", and the gallery
  itself was titled "Insert a component". They now all say **Add slide** ("Add a
  slide" for the gallery and the palette, "Add slide below" for the divider, which
  is the one launcher whose position is worth naming). The name describes the
  outcome — a new slide — rather than the mechanism.
