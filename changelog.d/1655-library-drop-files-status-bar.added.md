- **Drop a file straight into the Library.** A lattice-asset `.zip` imports and a reference file attaches, from one gesture, anywhere in the panel and on any filter tab. Drop routes to the two ingests the Import and Add-file buttons already used, so there is no second parser and no second set of size/kind rules; anything neither takes is named in a toast rather than silently ignored.
- **The "Docs" filter is now "Files".** A saved logo or knowledge file is a file, and anything that is a file can be referenced in a deck — the shelf now says so.
- **The Library has a status bar.** The "N total" count moved out of the filter row, where it competed with the five filter pills for one row and had to hide entirely on a narrow pane and on a phone. It now sits at the foot of the panel at every width, with the breakdown by kind and the disk the saved files are using.
