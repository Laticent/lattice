- Studio header: the ⌘K search pill no longer wraps to two lines and overflows the 54px top bar at 1280px in the Craft stop. It is now `shrink-0 whitespace-nowrap` in both headers, so the deck switcher stays the row's one shrinkable item as designed and the deck title truncates a little sooner instead.
- `studio-header-fit.spec.ts` now visits 1280px — the `xl` breakpoint it already named but stepped over, between 1100 and 1440 — and asserts that no header control is taller than the header. Every previous guard measured the row's width only, so a control that resolved a width shortage by growing taller passed them all.
