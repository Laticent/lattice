- Studio chrome now tracks the palette on every status surface. `--chart-2` / `--chart-3` / `--chart-4` were read in 49 places across 15 files and **defined nowhere in the repo**, so their hardcoded `#9c3f00` / `#2e6f00` / `#9a6a00` fallbacks won on all 36 palette × mode rows — the deterministic-finding severity glyph, the Draft/Approved/Edited lens chips, the component-gate validation glyphs, the On pace / Behind pace presenter chip, the notification count badge, the issue counts, and both syntax highlighters. They now read `--pass` / `--warn` / `--fail`, which every palette defines and which clear AA as text on all 36 rows (worst 4.52:1).
- The notification count badge takes `--warn-fill` rather than a foreground token, so its white text keeps AA in dark mode.
- Fixed the same defect class in six more places found while sweeping: `--muted-foreground` and `--card` (Tailwind bridge names, spelled `--color-*` in `@theme`, so they never resolved in a raw `var()`) in the Print drawer, Library, Joystick, SlidePicker, WorkspaceSheet and SlideContext; `--ok` in the Compose editor's pass mark; `--surface-raised` / `--background` in the playground; and the phantom `--fs-display` / `--fs-lead` in the Component studio's starter CSS, which silently dropped the `font-size` declaration in the example every new component starts from.
- **Added:** `checkDanglingTokenReads` (via `build:check`) fails any docs-site `var(--X)` whose token is declared nowhere in `docs/src` or the engine's CSS. This catches both endings — a hex fallback that wins forever, and a bare read that invalidates its whole declaration at computed-value time — and would have caught this and the `--db-sev-*` case fixed in #1684.
