- **Changed: the CI-green beacon no longer claims more than it checked.** It posted `✅ **CI green**` on the strength of four tiers of one workflow, which reads as "everything on this PR is green" — it is not. CodeQL runs from GitHub's default code-scanning setup, in a separate workflow the `ci` gate has no `needs` edge to. Observed twice on #1689: the beacon posted "CI green" while CodeQL had failed with 4 high-severity alerts, and again with 2 still open. It now leads with `✅ \`ci\` workflow green`, names the four tiers it covers, carries a snapshot of every other check run on the head commit — leading with ⚠️ and naming them when any has failed, and saying so when one is still running or could not be read — and states in the body that it is not a merge signal.
