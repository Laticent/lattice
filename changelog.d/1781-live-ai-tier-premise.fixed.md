- **Fixed: the Studio's live AI-architect E2E scenario can pass again.** Its per-finding "Fix" scenario had become permanently red: the Coach renders that chip only for a SLIDE-scoped finding, and the two-slide deck under test scored 95/100 with a single deck-level one, so the chip could never exist and the spec died on a 30-second locator timeout that looked like a broken AI affordance. The deck now trips a real slide-scoped finding, the spec asserts that finding before reaching for its chip, and the chip locator no longer assumes the model catalog has priced the connected model.
