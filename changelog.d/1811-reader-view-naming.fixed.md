- **Fixed: two places still called reader views "Lenses" to your face.** The
  tablet overflow menu offered "Lenses — reader views", and a Workspace settings
  note pointed at "the Lenses panel" — both entry points into a panel titled
  *Reader views*. The internal name stays internal: `lens` is the machine word
  (the `lenses:` front-matter block, the `_lens` tag, the library), and every
  string a person reads says "view".
