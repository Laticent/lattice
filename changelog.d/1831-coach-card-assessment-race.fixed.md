- **Fixed: a Studio Coach quick read no longer vanishes on its own moments after you open it.** The deck assessment runs on a 400ms debounce in the background, and every round replaced the whole findings list — which cleared the quick-read card outright, including the three reads (Structure, The ask, Pacing) that are computed from the deck source and cannot go stale when the findings are recomputed. Click a chip while a round was in flight and the card disappeared a few milliseconds later with nothing on screen to explain it. A round now clears only the two cards it actually invalidates (Top fixes, Weakest slide), and editing the deck clears the card at the keystroke rather than a debounce later. This was also the cause of the docs suite's last intermittent failure in the family tracked by #1328 and #1806 — the card was being deleted behind the wait, so no time limit could have fixed it.
