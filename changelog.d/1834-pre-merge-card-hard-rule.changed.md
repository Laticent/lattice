- **Changed: the pre-merge card is now HARD RULE #28, and it goes on the PR.** The card was
  a single row in CLAUDE.md's operating-mode table plus a `workflow.md` section, with nothing
  binding the two together — so a card could be, and was, built from the one-line summary
  instead of the contract, shipping an invented `medium-high` level, three per-issue
  confidences instead of one derived floor, no axis attribution and no raise path. #28 pins
  all four levers and requires the card to be posted as a PR comment as well as in the merge
  ask, so the evidence sits next to the diff rather than only in a chat transcript.
