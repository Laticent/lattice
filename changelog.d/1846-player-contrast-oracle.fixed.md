- **The `contrast:player` oracle is re-blessed** — 29,098 runs across 142 decks, stale since
  #1789. **46 findings dropped** (no longer reproduce), **5 improved**, **0 held at the
  committed value**, and 3 entries are new to the file but reproduce identically on `main`
  with this branch's changes absent, so they are pre-existing rather than regressions.
