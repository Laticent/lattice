- **Fixed: three more verdict-grid / pricing card shapes rendered raw `[x]` markdown to a reader** through the runtime while the engine badged them. The engine's rule is list-kind-agnostic and depth-unbounded; the runtime read `ul` only, one nesting level down, through `textContent`. So a **numbered** card, a card whose criteria are **numbered**, and a criterion carrying its **own sub-criteria** all diverged — measured 2 badges against 0, 2 against 0, and 2 against 1. Verified on the real surface: rendered through marp-cli and opened in Chromium, the pre-fix runtime shows 0 badges and five raw markers on the slide where the engine shows 5.
- **Fixed: badging a criterion that had sub-criteria DELETED them.** `replaceChildren(badge)` dropped the nested list outright, so every sub-criterion vanished from the slide. The badge now preserves the sublists.
- No committed deck changes — nothing in the tree numbers a verdict-grid card or nests three deep, and the common "last nested item is prose" card renders identically (32 committed slides checked, 0 differ).
- **Known, and not addressed here:** the two shapes this unblocks have no card chrome. `verdict-grid.styles.css` and `pricing.styles.css` select `> .cell-stage > ul > li > ul > li` — bullet lists, depth 2 — so a numbered card or a depth-3 criterion now badges *consistently on both paths* but renders unstyled. Closing the divergence and styling the shapes are separate jobs.
