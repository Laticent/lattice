- **A gate now holds adjacent categorical slots against erosion.** `test/unit/palette/cat-adjacency-floor.test.js` freezes the distance between neighbouring `--cat-N-*` slots for every shipped palette, on both tiers, and fails when one gets closer. Nothing measured this before: `cvd-audit` excluded pale-fill pairs because "contrast-audit covers normal distinctness", `contrast-audit` said the curated slots are "NOT re-audited here", and the tool it pointed at only compares a slot against itself.
- **`design/theming.md` now says which tier carries the category, and that the two swap by canvas.** On a light ground the mark is saturated and the fill is a wash; on a dark ground they trade places. Measuring the ramp by token name therefore reads the wash in one mode and the category code in the other — a factor of twelve on `indaco`.
- **It also states what the cycle does and does not guarantee.** Each slot is guaranteed against its own ground; mutual distinctness between slots is a ratchet with 75 readings still below the reference floors. Author for six categories rather than twelve.
