- `engineering/decisions/2026-09-02-alarm-channel-saturation.md` records that the nightly alarm family's live defect is the opposite of the silent night, and that finding reverses the order the remaining work should be done in. Two rolling threads file every night and neither has ever closed: **#1532** (perf) has 21 comments over 23 days, **#1845** (integration) has 9 since 08-25. #1532 cannot close by construction — `perf-nightly.yml`'s `watch` job has no stand-down step at all, because #1988 added one to `engine-perf` and missed its sibling. It is also firing on noise, and the note **proves** it from the alarm's own thread: three consecutive runs measured the *identical* commit pair (`b4d202c` → `0c920ca`) and disagreed by up to **100KB** of script on the same page — the head commit reading 209, 151 and 251KB on mobile `/` across three nights of unchanged bytes. On 08-30 that noise filed a ❌ regression on the exact row that passed ✓ on 08-28. `script-size` is classified deterministic with a 3% tolerance but is summed from Lighthouse network records, so it measures what happened to load, not what the build produced. The note argues the silent-night fix should therefore go **second**: measured, it buys ~1 extra issue a month (1 red scheduled run in 107 across its four workflows) delivered into a channel already carrying ~2 false alarms a night.
