- **Fixed: `check:chart-fit` reported success when it had measured nothing.** With no Chromium found it printed "SKIPPED, nothing verified" and exited **0**. Nothing runs the script today, so this changed no behavior — but it was one decision away from being wired, and on a runner whose puppeteer cache restored without the binary the job would have gone green nightly while every clip went unmeasured. It now exits 2, matching `check-geometry-parity.js`; 2 rather than 1 keeps a setup failure distinct from a real clip finding.
- `engineering/decisions/2026-09-01-orphaned-check-census.md` records what is actually unguarded. Of five checks reported as orphaned, three (`check:responsive`, `equiv:check`, `oracle:check`) have their logic run by something already wired — two by unit tests in `npm test`, one by `checkSplitOracle` in `build:check` — so wiring them would duplicate an existing gate. Only `check:chart-fit` and `geometry:check` are unchecked by anything. The note carries measured runtimes, current verdicts, determinism risk, and three wiring options with costs; wiring is left for a decision, since adding a job or step is the CI-contract change reserved for the repo owner.
