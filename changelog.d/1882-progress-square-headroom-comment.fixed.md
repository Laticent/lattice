- **Fixed: a comment in `progress.styles.css` asserted a measurement that was false by 189px.** It justified excluding `data-family="square"` from the portrait STACK arm on the grounds that "square already fits (measured -178.1px of headroom)". Square does not fit: `.progress-bars` measures 677.8px inside a 666.9px stage, so 10.9px paints below `.cell-stage` and is cut silently by `overflow: clip`. The comment now records the real measurement, the mechanism (at square `--fs-message` resolves to 37.8px in a 182.25px label column, wrapping four of five labels to two lines), and where the clip is already tracked. No layout changed — `check:chart-fit` reports the same single clip before and after — because choosing among the levers that clear it is a design call about how `progress` reads on a square canvas, not a tuning. The stale claim mattered: it was the stated reason the exemption existed, so the next reader would have re-derived it from a number that had stopped being true.
