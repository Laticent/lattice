- The `redline` component's own summary line rendered its example `<ins>`/`<del>` as live elements, so the words "ins" and "del" were eaten out of the sentence on the gallery cover and the rest of the line displayed underlined and struck through — a fake tracked change on the summary of the component about tracked changes. Backticked, matching how every other component's docs refer to a tag.
- The rendered-slide contrast audit no longer scores screen-reader-only text as ink. The house idiom for it — a 1×1 box with `overflow:hidden` and a zero-area clip — is neither `display:none` nor `visibility:hidden` (either would defeat its purpose), so `.cell-sr-label` and `.lattice-description` were being measured against a backdrop for glyphs the renderer never draws.
- Fence scanning now sees a deeply indented fence. A fence inside a list item is indented past CommonMark's three-space top-level cap, so a ```` ```powershell ```` in a list was invisible to the language scan: the Playground never fetched the grammar and the fence stayed monochrome there while the CLI export colored it — the exact cross-surface gap the on-demand grammar loading exists to close.
- A fence tagged with a JavaScript prototype name (```` ```constructor ````, ```` ```__proto__ ````) no longer resolves to a bogus grammar URL. It injected `<script src=".../hljs/undefined">`, 404'd, and was retried on every render — once per debounced keystroke in the Studio.
