- **Fixed: `progress` painted 10.9px outside its stage at square, and was silently cut.** `.progress-bars` measured 677.8px inside a 666.9px stage, and the stage is `overflow: clip`, so the overflow never showed — the runtime overflow probe reads `dy=11` against a tolerance of 12, and no committed square deck uses the component, so `check:chart-fit` was the only thing that ever reported it. At square the deck's `--canvas-scale` is 1.65, which steps the label to `--fs-message` (37.8px) inside a 182.25px column, wrapping four of the five labels to two lines. The `data-family="square"` arm now joins the STACK layout that `tall` and `strip` already use: the label takes its own full-width row above the bar, left-aligned at `--fs-body`, and the bar gets the full width. `node tools/check-chart-fit.js` now exits **0** across landscape, portrait and square with no `SANCTIONED_CLIPS` entry — the gate is green for the first time in a while. Portrait, story and mobile are unchanged and render pixel-identical (with square as a positive control at 202,211 differing pixels, so the zeros are evidence rather than a broken comparison); only square moves. Extending STACK to square also made its family list identical to the FILL arm's, leaving three pairs of rules sharing a selector byte-for-byte — which both blinded `check-family-tiers.js` (it keys its A/B maps by selector text, so colliding rules overwrite each other) and left two dead declarations that STACK always overrode. The two arms are merged here: 6 distinct family rules where there were 9 collided ones, no rule a no-effect, and no rendered change (0 differing pixels before vs after the merge).
