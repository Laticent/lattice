- **`code` slides now take a below-note and an annotation**, like every other layout that does not claim its trailing paragraph. The layout had reserved that paragraph for a caption whose CSS selector could never match the engine's output (`section.code > .cell-stage > em`, while markdown always wraps an italic-only line in a `<p>`), so the reservation bought nothing and cost two universal blocks. A trailing paragraph on a `code` slide had been rendering as unstyled body text inside the code stage; it now docks in the coda band under its hairline, where the same block already lands on `content` and `compare-code`. Key Insight (a trailing blockquote) worked before and still does.
