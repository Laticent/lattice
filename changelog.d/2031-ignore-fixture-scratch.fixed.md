- **Fixed: a size-sweeping gate's scratch decks showed up as untracked files.** `check-chart-fit.js` renders the fixture at three deck shapes by rewriting its front matter and writing `.chart-fit-<size>-<pid>.md` *next to* `test/fixtures/chart-fit.md`. It cleans those up on a clean exit, but its own comment records that an early exit "would strand the per-size scratch decks in `test/fixtures/`" — and that is the path that always runs while a gate is being fixed. Anyone reproducing a clip by hand leaves the same shape behind. None of these files is ever tracked, so an abandoned one only served to trip the repo's commit-and-push hook. `test/fixtures/.*.md` is now ignored; no tracked file matches the pattern.
