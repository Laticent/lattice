- **Fixed: `roadmap` clipped 75.4px at portrait, and a shipped deck clipped five pages, as soon as the split gained a universal forward pointer.** #2016 made the split's `next: …` signal universal — a component declaring no `capacity.relationship` now defaults to `sequence` instead of being skipped — and that signal is seated **inside** `.cell-stage`. `roadmap` is the tree's only component that floors its chart body at `min-height: 100%`, so the stage was already exactly full when the sibling arrived: 801.25 + 13.16 gap + 62.2 signal = 876.6 in an 801px stage. Two rules that were each correct alone. The page count never changed and nothing failed to split; a new element simply landed in a full box. The floor is now `flex: 1 0 auto`, which says the same thing declaratively — **grow** into whatever free space the signal did not take, so an under-full split page still fills (measured: body 725.9px, signal sitting exactly at the stage bottom), and **never shrink**, so an overstuffed body still exceeds the stage and `overflow-probe.js` still fires (measured: 1666.7px body in an 893px stage, probe true). Subtracting a fixed band would have been wrong — the signal wraps to two lines in a narrow box, so the band is not a constant. `check:chart-fit` returns to exit 0 across all three sizes with no `SANCTIONED_CLIPS` entry, and the overflow corpus returns to its baseline of 7 (`examples/portrait-roadmap.md`'s 5 regressed pages are closed). The `strip` family was clipping worse than portrait — **80px** — and is also closed.
- **Also fixes a pre-existing clip nobody had reported.** When a roadmap slide carries a trailing paragraph, it renders as a `.chart-caption` inside the same stage, giving the stage a third child. On the run's last page — the one with no forward-pointer signal, so the page this change was not aimed at — that caption was clipped by **237.2px** with the probe reading `true`. The same `flex: 1 0 auto` closes it to 0. Found by an independent checker probing cases the change did not claim, not by the gate.
- Corrects three records that #2016 left describing a tree that no longer exists: `roadmap.manifest.json`'s `capacity.note` and `split.note` still promised a four-page budget (`ceil(N/4)`, `evenGroups`) that #2016 deleted; `check-chart-fit.js`'s `SANCTIONED_CLIPS` comment claimed portrait roadmap's clip was closed for good; and `2026-09-01-orphaned-check-census.md` asserted the gate was **green and safe to wire** — a claim that was already false when it merged, because the branch that wrote it measured a tree without #2016 on it and the gate it describes runs in no CI job.
