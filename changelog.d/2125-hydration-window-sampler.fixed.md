- **Fixed: the Playground's hydration-gate E2E test can hold its window open again.** The spec that proves a Galleries click waits for the island to hydrate — not merely for the button to appear — read `body[data-view]` once from the runner right after a `domcontentloaded` navigation. Deferred module scripts block DOMContentLoaded, so on a production-shaped build that milestone had moved to the far side of hydration and the spec failed on its own self-check, 6 runs in 6. It now samples the trigger's first box and the island's first `data-view` per frame from inside the page, so the window is measured on the page's own clock.
