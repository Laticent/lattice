- Re-blessed the six committed example decks this change actually moves: `adaptive-sizing`
  (28 → 32 pages), `portrait-journey` (15 → 18), `portrait-prose-deboost` (31 → 35),
  `portrait-roadmap` (13 → 15), `split-structure` (26 → 27) and `split-horizontal` (same page
  count, 36 text lines — its own caption, which the deck says "belongs on the cover, once", now
  appears once instead of on every body page).
