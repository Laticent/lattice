- An over-stuffed member on a coverless `split-panel` split page no longer paints under the forward-pointer pill. The reserved band is padding, and `overflow: clip` clips at the padding box, so a long member body spilled straight back through the reserve and printed beneath the pill — measured at 396.1×51.5px on a portrait page — with **no** overflow class, no `clip-marked`, and a clean `lint:deck`. The member list now clips at its own edge, above the band, and because the overflow probe folds a child's internal overflow onto the panel it measures, the engine flags the page and runtime auto-split can give the member another page instead.
