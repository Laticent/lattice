- A coverless `split-panel` split page on a **Form frame** (`<!-- _class: split-panel pullquote form -->`) drew its forward pointer off the left edge of the slide when the next page's title approached the engine's 42-character label cap — the rounded cap gone, the border cut flush at the slide edge and the first glyph sheared, with no overflow warning and a clean `lint:deck`. The pointer's width clamp was losing the cascade to a Form-scoped rule in `base.modifiers.css` at equal specificity and later source order, so it never applied on Form pages at all.
