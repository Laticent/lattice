- The heatmap's per-cell value ink is now pinned by a render test on both canvases, so the four sanctioned blacks it adds to the scoped-render guard cannot hide a real dropped color. `heatmap` is the only chart member whose text ink is chosen per mark rather than per canvas — a faint cell and a saturated one take opposite inks, and which is which inverts between light and dark — so it paints opaque black in every theme × scheme cell, where every other sanctioned black in that ledger is light-only.
