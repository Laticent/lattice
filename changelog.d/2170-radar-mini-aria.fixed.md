- The small-multiples `radar` now hides its marks from the accessibility tree like every other chart. The family-wide pass missed it: that variant emits one `<svg role="img">` per series, and the test guarding the pass asserted only that an `aria-hidden` group appeared *somewhere in the document*, which one wrapped root satisfies for a whole slide. Measured on the shipped radar gallery, 5 of 15 roots still exposed their subtree and four were these minis — a reader heard the series name, then walked its axis names and the name again. Each mini keeps its own accessible name.
- The guard is now a per-root scan rather than a document-wide match, and it covers `radar small-multiples`, `heatmap` and `map`, which the first pass never exercised.
