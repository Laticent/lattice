- **Fixed: a walkthrough running on a phone no longer reveals the line it just typed behind the software keyboard.** The band a tour's caption covers is published against the layout viewport, which iOS does not shrink for the keyboard — so a host clearing only that band cleared the wrong edge and landed the reveal behind the keyboard anyway. The Studio's `tourChromeOverlap` now clears whichever obstruction reaches higher, and `docs/src/lib/vetrina/README.md` carries the recipe for any host doing the same. The published `--vt-chrome-top` / `--vt-chrome-bottom` contract is unchanged.
