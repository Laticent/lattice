- **Four chart narrators now fire on the slides they were written for.** `radar`, `quadrant`, `state-chart` and plain `journey` each reached no narrator at all on their own shipped sample, so the slide fell back to a generic walk of the rendered SVG. Measured on the real `--captions` export, as a fraction of the source a slide actually speaks: radar 0.12 → 0.89, quadrant 0.12 → 1.04, state-chart 0.42 → 1.05, journey 0.76 → 1.07.
  - `radar` and `quadrant` bailed entirely when the eyebrow already stated the scale — throwing away every series, item and coordinate to avoid one duplicated sentence. They now skip just the redundant sentence.
  - `state-chart` returned nothing when a deck tagged its own start and end states, leaving the flattener to read `submit => 2` as glyphs. It now reads the machine by name: "From In Review, approve goes to Approved, reject goes to Draft, and revise stays here."
  - A plain `journey` had no narrator at all — only the `weighted` variant did — so a listener got the SVG legend in DOM order: "prospect. sales. user. onboarding. Pain. 1. 2. 3. 4. 5. Delight." It now reads each step with its actors and its 1–5 mood, and leaves an omitted mood unsaid rather than reporting the neutral default the chart plots.
