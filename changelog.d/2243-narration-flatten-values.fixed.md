- **A chart bullet no longer loses its value to the narrator.** The base narration flattener appended a sentence terminator to each structural line and then stripped ordered-list markers from the *joined* text, so a bullet's own whole-integer value (`- First \`120\``) turned into something shaped like a list number and was deleted. Measured: three bullets narrated as "First Second Third 31." — every value but the last silently gone. Markers are now stripped per line, where they are unambiguous, which also stops a mid-sentence `-` or `>` being mistaken for a bullet or a quote marker.
- **A label and its value are no longer welded into one word.** The speech walker read bare `textContent`, which joins adjacent elements with nothing, so the roadmap spoke "ShippedSignal taxonomy" (nine times on one slide), kanban "Shipped workL", and the contact card "AdaSlide".
