- **A state chart no longer reads a state's tint as part of its name.** `2. Accepted \`done\`:::state-pass-hue` narrated as "Accepted `done`:::state-pass-hue" — the tint is a style hook, not a name, and because it sits after the pill's closing backtick the pill-stripper never saw it. Both the state's own line and every transition that names it now read "Accepted". A deck's prose *about* the `:::token` syntax is untouched, since that is slide text the author wrote.
