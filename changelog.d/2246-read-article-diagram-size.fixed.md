- **Fixed: a Mermaid diagram in a Read · Article is drawn at its natural size, centered.** It was being blown up to fill the figure band — a two-node flowchart stretched to 1100x310 with labels four times the size of the body text beside it. The diagram's own viewBox is the size to use, and it also decides which axis binds: a wide left-to-right chart runs out of column width first, a tall top-to-bottom one runs out of height first and shrinks proportionally. Diagrams in the Studio's reading view, in a `--read` export and in a shared `--player` deck now match.
- In the `--read` export's article, a figure image and a figure caption are centered too, which is what the player's article and the Studio's pane already did.
