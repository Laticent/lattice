- **A split cover's eyebrow clears AA.** All six covers set `opacity: 0.85` on `var(--on-accent)`, which over a **light** accent lands at **4.40:1** — just under the 4.5 floor, and the `--player` HTML export's light toggle is where a reader meets it. The opacity is gone; the demotion is carried by size and tracking, which is what `base.tokens.css` prescribes for this exact case ("if you need a quieter label here, spend size or weight, not alpha" — seven of the fourteen palettes are sub-AA on the 70% `--on-accent-secondary` tier, so the obvious token swap would have been worse).
- **Inline code on a cover was invisible.** The chip's default ink is `var(--code-inline-fg, var(--accent))`, so on an accent field it was the accent on the accent: measured, indaco, **1.16:1** as exported and **1.00:1** after the player's dark toggle. The six cover classes join the dark-island list in `base.modifiers.css` with `--code-inline-fg: var(--on-accent)` — the token every theme curates for AAA against its own accent. Reached through the split envelope, which hoists an authored masthead onto the cover verbatim, inline code included.
- **`topic` joins that list too**, for the same reason it was missing from `base.finish.css` until #2294: it paints `--surface-inverse` in every mode and nobody had named it. Measured on the player's light toggle: **1.79:1**, the brand accent on the inverse panel.
- Verified with `tools/check-player-contrast.js` on three decks carrying every cover the splitter emits, in **both** toggle states: `dark-canvas-ownership` 2 → **0** below AA, `finish-canvas-light-half` 4 → **0**, `spectrum-canvas-ownership` 3 → **0**. 358 runs, 0 below AA.
- **Not fixed here, and filed:** *any* paragraph containing exactly one inline-code span is styled as a subtitle, because `:only-child` counts element children and ignores text nodes — so `<p>The <code>background:</code> shorthand clears every longhand.</p>` matches `section h2 + p:has(> code:only-child)` and takes `--text-secondary`, a light-canvas ink, onto a dark panel. Measured at **1.65:1** on a `topic` slide. The rule's own comment says "a paragraph whose only child is an inline code span", so the selector does not express the intent it documents. Tracked as #2308.
