- A paragraph that holds one inline code span alongside prose now renders as body prose. CSS `:only-child` cannot see text, so an ordinary sentence like "The `background:` shorthand clears every longhand" after a heading was re-typeset as a muted italic subtitle and lost its code pill. On a `topic` slide its canvas ink measured 1.65:1 on the panel in the `--player` light toggle. The engine now marks such a paragraph (`data-prose`, `lib/core/prose-code.js`, mirrored by the runtime), and all 96 `:has(> code:only-child)` rules skip it at zero added specificity. A real eyebrow or subtitle, written wholly in backticks, is unchanged (#2308).
- 35 paragraphs in 28 shipped decks relied on the old behavior. All but one were sentences; the one intentional kicker, in `examples/overflow-guards.md`, now wraps the whole line in backticks and renders as before.
- The same mark applies to a tight list item, which changes one component gallery: `authority-chain branching` drew its first branch (`` `15 U.S.C. §6501` COPPA, 1998 ``) as a bare label because it matched the chip rule for a code-only item, while the other four branches were pills. All five are now pills.
