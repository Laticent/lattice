- `tools/chart-mark-separation.js` asks whether a rendered chart still separates its categories under a colour-vision condition — scoring each mark's distance from its nearest neighbour AND the spread of its own shading, so a mark that paints over its own categorical read is told apart from a palette that simply collapses. Credits the texture and stroke-dash channels rather than scoring colour a chart does not depend on.
