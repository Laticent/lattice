- **Studio preview: a diagram slide no longer shows its Mermaid source before the diagram.** Typing on a diagram slide repainted the raw ```mermaid fence for ~160ms on every keystroke — 10 painted frames, on an idle machine — with the finished SVG already in the runtime's cache. Three fixes: an un-tagged fence now paints nothing in a document that has told us a runtime is coming (the slot it reserves is unchanged, so nothing on the slide moves; an export, whose runtime is stripped, still shows the source), the runtime hands back a cached diagram in the same task as the swap that brought the slide in rather than 150ms later, and the Studio — the editor preview and Present alike — keys its preview frame on whether the DECK has a diagram rather than the shown slide, so moving onto a diagram slide patches the preview instead of rebuilding it. Measured on the built Studio: raw-source frames 10 → **0**, and the diagram is back in **20ms** instead of 172ms (20ms instead of 368ms when navigating). A diagram nothing has rendered yet still takes its render time, now behind an empty slot rather than its source.
- Fixed a latent cache defect this exposed: `diagramScopeKey` keyed a rendered diagram on the section's raw inline `style`, which the runtime itself stamps (`--_sec-1cqi`, the deck-logo placement) — so the same slide produced two different keys either side of that stamp. The key now reads the CSSOM's own serialization (`style.cssText`) instead of the attribute — the browser rewrites values, not just whitespace, once anything calls `setProperty` — and drops the properties the runtime writes.
