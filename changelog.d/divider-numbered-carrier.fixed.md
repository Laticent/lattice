- **Fixed: the `numbered` bookend modifier stamps its index again — on every render path, and alongside `silent`.** `divider numbered` / `closing numbered` wrote the numeral onto `section::after`, the pseudo the engine reserves for the page number. Two owners took it back: `packTheme` comments out any non-pagination `content` on a `section…::after` rule, so the counter was stripped from the browser-path stylesheet the docs Playground, the Studio and `lib/runtime` load (the emulator/CLI PDF kept it — one modifier, two pictures); and `silent` / `no-paginate` null the same pseudo at an equal `(0,2,2)` specificity from a later file, so `divider silent numbered` — the sample shipped in `divider.docs.md`, the manifest and the gallery deck — rendered nothing anywhere. The numeral now rides the slide heading's `::after`, which neither owner can reach. It sits in the same slide corner and no longer suppresses the page number, so a paginated `divider numbered` shows both.
