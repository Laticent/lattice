- **The `logo` and `watermark` Tiles now declare where they actually render.** Both manifests claimed a band Cell — `logo` said `masthead-bay`, `watermark` said `stage` — while the render pins each to the section box: `base.modifiers.css` positions the mark at the frame inset with `logo-x`/`logo-y` setting its center as a percent of the whole slide, and `watermark.transform.js` appends the ghost to the section. Both now dock in a new `slide` Cell that models the section box, so `dist/docs/forms.json` and `design/forms.md` describe the composition a reader actually gets. No rendered slide changes — nothing outside the Form validators reads `Tile.fits`.
- The Cell `region` vocabulary was defined in **three** places — the enum in `schema/cell.schema.json`, `CELL_REGIONS` in `lib/forms/index.js`, and the slicing-relocation enum in `schema/frame.schema.json` — with nothing keeping them in step; adding a region passed one and threw in another. All three now carry the same list (`coda` was already missing from the third), and a test compares them.
