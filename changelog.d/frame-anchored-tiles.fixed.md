- **`logo` and `watermark` now name the Cell they actually render into.** Both Tile manifests claimed a band Cell that nothing ever docked them in — `logo` said `masthead-bay`, `watermark` said `stage` — while `base.modifiers.css` and `watermark.css` position both against the section box. The manifests said one thing and the render did another, a divergence invisible for as long as a single Frame existed.
- **They get a Cell each, not a shared one.** The two have exactly one property in common — neither docks in a band — and it is a negative one; every other Cell in the catalog names a position. A shared Cell forced a single `z`, `accepts` and `capacity` onto two Tiles that agree on none of them, and made per-Tile suppression unexpressible, because a Frame suppresses by Cell id: no Frame could drop the watermark while keeping the logo. `logo` is content-plane and accepts `chrome`; `watermark` is atmosphere-plane and accepts `surface`; both are `capacity: "one"`, `fill: "anchor"`.
- No rendered output changes — these manifests are the model's description of where Tiles live, and both Tiles already rendered where they now say they do.
