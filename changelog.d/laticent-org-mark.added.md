- A brand mark for the **Laticent** org, in `design/logo/laticent/` — a slate `L` with a brass seam let into it, shipped as an adaptive mark, a favicon-size minimal variant and light and dark lockups, plus `generate.py` to rebuild them and `audit.py` to check every asset against a round-avatar crop. Nothing outside that directory changes yet.
