- The **Laticent** primary symbol is now a slate tile with the `L` reversed out of it. Every product mark places its hub at exactly (64,64), so the family is organized around a centered hub and a letterform cannot join it — transplanting a hub into the `L` lands the node 38 units off center. The parent instead differs by class: a contained mark beside five free-standing ones reads as the thing they live inside. The tile is also the only form whose content clears the Android maskable safe circle (43.7 against 51.2). `audit.py` now recognizes tiles and is transform-aware.
