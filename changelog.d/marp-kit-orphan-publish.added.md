- **`dist/marp-kit` is now published to the orphan `dist-marp-kit` branch** after every relevant push to `main` (`.github/workflows/publish-marp-kit.yml`), so the copy-and-go Marp kit is browsable and raw-fetchable straight from the repo again — the surface lost when `dist/` stopped being committed to `main`. npm install and the GitHub Release zip already carried it; this restores the third.
- `dist/marp-kit` now ships **unminified counterparts** of its Lattice-authored files (`lattice.css`, `lattice-runtime.js`, `cuoio.css`, `cuoio-dark.css`) alongside the `.min` files the kit's configs actually run on, for anyone who wants to read or diff what they took.
