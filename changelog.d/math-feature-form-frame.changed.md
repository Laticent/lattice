- The `math` layout begins moving off its sovereign frame onto the shared Form frame, one variant per commit. **`feature` and bare `math` move first.** They gain the masthead band, the `meta:` strip, the progress rail and the watermark tile — none of which a sovereign math slide could ever render, so `meta:` on a math slide silently did nothing until now. The body layout is unchanged: hero equation left, `where` legend right.
- Two visible differences on a migrated math slide: the heading now sits in the masthead band rather than in the layout's own grid, and it gains the masthead's hairline (`.cell-masthead` carries a `border-bottom`, which every other Form slide already has).
- The seven remaining variants — `derivation`, `theorem`, `compare`, `canvas`, `matrix`, `stats`, `decompose` — render **byte-identically** and keep their sovereign frame until their own commit. A slide that opts out with `no-form` keeps the old shape too.
