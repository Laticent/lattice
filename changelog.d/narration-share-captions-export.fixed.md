- **Fixed: the Studio's "Captions (.vtt)" download was failing outright.** The generated `read-along-core` bundle the docs site loads did not re-export `emphasisForResolved`, so the export threw `_.emphasisForResolved is not a function` and produced no file. Caught by driving the real Share sheet; no unit or type check could see it, because the bundle's export list is assembled in `tools/build-read-along-core.js` rather than imported.
