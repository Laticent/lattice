- A Mermaid diagram in a Read · Article now appears at its natural size, centered, and scales down to fit the column instead of being blown up to fill it. Maximizing the width stretched a two-node flowchart to 1100x310 with labels four times the size of the body text beside it; the diagram's own viewBox is the size to use, and it also decides which axis binds — a wide left-to-right chart runs out of column width first, a tall top-to-bottom one runs out of height first. Diagrams in the Studio's reading view and in a shared `--player` deck now match.
