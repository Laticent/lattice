- `engineering/development.md` §Context cost now sizes the `MAX_THINKING_TOKENS` lever instead of only naming it: thinking is 33.6% of output tokens and 5.6% of the bill, measured over 77 turns. §Re-measuring gains the script that produces it, with the three traps that each produced a wrong number first — the transcript stores no thinking text, one API message spans several records that repeat the same usage, and a mid-stream record carries a partial `output_tokens`.
- Recorded the measurement, the priced cap options and a recommendation against a repo-wide default in `engineering/decisions/2026-09-22-thinking-is-a-third-of-output.md`.
