{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main Studio window. App commands (save_file) need no entry here; plugins are called from Rust only, so none are exposed to the page.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
