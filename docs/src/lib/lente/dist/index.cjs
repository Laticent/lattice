/* Auto-generated by tools/build-lente-lib.js — DO NOT EDIT.
   Source: docs/src/lib/lente/*.ts. Rebuild: npm run lente-lib:build */
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// docs/src/lib/lente/index.ts
var index_exports = {};
__export(index_exports, {
  FULL_LENS_ID: () => FULL_LENS_ID,
  applyTag: () => applyTag,
  approvalHash: () => approvalHash,
  catalogFromComponents: () => catalogFromComponents,
  emitRegistry: () => emitRegistry,
  emitRegistryDelta: () => emitRegistryDelta,
  isPristineInherited: () => isPristineInherited,
  lens: () => lens,
  lensEligibility: () => lensEligibility,
  lensIndices: () => lensIndices,
  lensPairs: () => lensPairs,
  lensSlides: () => lensSlides,
  parseLensRegistry: () => parseLensRegistry,
  parseSlideTags: () => parseSlideTags,
  readerLenses: () => readerLenses,
  rebaseLensTags: () => rebaseLensTags,
  sha256Hex: () => sha256Hex,
  suggestMembership: () => suggestMembership,
  taggedLensIds: () => taggedLensIds,
  unknownLensTokens: () => unknownLensTokens,
  upsertLensRegistry: () => upsertLensRegistry,
  validateRegistry: () => validateRegistry
});
module.exports = __toCommonJS(index_exports);

// docs/src/lib/lente/hash.ts
var K = [
  1116352408,
  1899447441,
  3049323471,
  3921009573,
  961987163,
  1508970993,
  2453635748,
  2870763221,
  3624381080,
  310598401,
  607225278,
  1426881987,
  1925078388,
  2162078206,
  2614888103,
  3248222580,
  3835390401,
  4022224774,
  264347078,
  604807628,
  770255983,
  1249150122,
  1555081692,
  1996064986,
  2554220882,
  2821834349,
  2952996808,
  3210313671,
  3336571891,
  3584528711,
  113926993,
  338241895,
  666307205,
  773529912,
  1294757372,
  1396182291,
  1695183700,
  1986661051,
  2177026350,
  2456956037,
  2730485921,
  2820302411,
  3259730800,
  3345764771,
  3516065817,
  3600352804,
  4094571909,
  275423344,
  430227734,
  506948616,
  659060556,
  883997877,
  958139571,
  1322822218,
  1537002063,
  1747873779,
  1955562222,
  2024104815,
  2227730452,
  2361852424,
  2428436474,
  2756734187,
  3204031479,
  3329325298
];
var rotr = (x, n) => x >>> n | x << 32 - n;
function utf8Bytes(str) {
  const out = [];
  for (let i = 0; i < str.length; i++) {
    let c = str.charCodeAt(i);
    if (c < 128) out.push(c);
    else if (c < 2048) out.push(192 | c >> 6, 128 | c & 63);
    else if (c >= 55296 && c <= 56319) {
      const c2 = str.charCodeAt(i + 1);
      if (c2 >= 56320 && c2 <= 57343) {
        i++;
        c = 65536 + ((c & 1023) << 10) + (c2 & 1023);
        out.push(240 | c >> 18, 128 | c >> 12 & 63, 128 | c >> 6 & 63, 128 | c & 63);
      } else out.push(239, 191, 189);
    } else if (c >= 56320 && c <= 57343) out.push(239, 191, 189);
    else out.push(224 | c >> 12, 128 | c >> 6 & 63, 128 | c & 63);
  }
  return out;
}
function sha256Hex(input) {
  const h = [1779033703, 3144134277, 1013904242, 2773480762, 1359893119, 2600822924, 528734635, 1541459225];
  const bytes = utf8Bytes(input);
  const bitLen = bytes.length * 8;
  bytes.push(128);
  while (bytes.length % 64 !== 56) bytes.push(0);
  for (let i = 0; i < 4; i++) bytes.push(0);
  bytes.push(bitLen >>> 24 & 255, bitLen >>> 16 & 255, bitLen >>> 8 & 255, bitLen & 255);
  const w = new Array(64);
  for (let off = 0; off < bytes.length; off += 64) {
    for (let i = 0; i < 16; i++) {
      w[i] = bytes[off + i * 4] << 24 | bytes[off + i * 4 + 1] << 16 | bytes[off + i * 4 + 2] << 8 | bytes[off + i * 4 + 3];
    }
    for (let i = 16; i < 64; i++) {
      const s0 = rotr(w[i - 15], 7) ^ rotr(w[i - 15], 18) ^ w[i - 15] >>> 3;
      const s1 = rotr(w[i - 2], 17) ^ rotr(w[i - 2], 19) ^ w[i - 2] >>> 10;
      w[i] = w[i - 16] + s0 + w[i - 7] + s1 | 0;
    }
    let [a, b, c, d, e, f, g, hh] = h;
    for (let i = 0; i < 64; i++) {
      const S1 = rotr(e, 6) ^ rotr(e, 11) ^ rotr(e, 25);
      const ch = e & f ^ ~e & g;
      const t1 = hh + S1 + ch + K[i] + w[i] | 0;
      const S0 = rotr(a, 2) ^ rotr(a, 13) ^ rotr(a, 22);
      const maj = a & b ^ a & c ^ b & c;
      const t2 = S0 + maj | 0;
      hh = g;
      g = f;
      f = e;
      e = d + t1 | 0;
      d = c;
      c = b;
      b = a;
      a = t1 + t2 | 0;
    }
    h[0] = h[0] + a | 0;
    h[1] = h[1] + b | 0;
    h[2] = h[2] + c | 0;
    h[3] = h[3] + d | 0;
    h[4] = h[4] + e | 0;
    h[5] = h[5] + f | 0;
    h[6] = h[6] + g | 0;
    h[7] = h[7] + hh | 0;
  }
  return h.map((x) => (x >>> 0).toString(16).padStart(8, "0")).join("");
}

// docs/src/lib/lente/tags.ts
function fenceMarker(line) {
  let i = 0;
  while (i < line.length && (line[i] === " " || line[i] === "	")) i++;
  const ch = line[i];
  if (ch !== "`" && ch !== "~") return null;
  let j = i;
  while (j < line.length && line[j] === ch) j++;
  return j - i >= 3 ? line.slice(i, j) : null;
}
function fenceRanges(src) {
  const out = [];
  let offset = 0;
  let open = -1;
  let marker = "";
  for (const line of String(src ?? "").split("\n")) {
    const m = fenceMarker(line);
    if (open < 0) {
      if (m) {
        open = offset;
        marker = m;
      }
    } else if (m && m[0] === marker[0] && m.length >= marker.length && line.trim() === m) {
      out.push([open, offset + line.length]);
      open = -1;
    }
    offset += line.length + 1;
  }
  if (open >= 0) out.push([open, String(src ?? "").length]);
  return out;
}
var inFence = (i, ranges) => ranges.some(([a, b]) => i >= a && i < b);
function isBlank(s) {
  for (const c of s) if (c !== " " && c !== "	" && c !== "\n" && c !== "\r") return false;
  return true;
}
function findDirectiveComment(src, key, ranges) {
  const text = String(src ?? "");
  const fences = ranges ?? fenceRanges(text);
  const marker = `_${key}:`;
  let i = 0;
  for (; ; ) {
    const open = text.indexOf("<!--", i);
    if (open < 0) return null;
    const close = text.indexOf("-->", open + 4);
    if (close < 0) return null;
    i = close + 3;
    if (inFence(open, fences)) continue;
    const inner = text.slice(open + 4, close);
    const k = inner.indexOf(marker);
    if (k >= 0 && isBlank(inner.slice(0, k))) return { start: open, end: close + 3, body: inner.slice(k + marker.length) };
  }
}
function allDirectiveBodies(src, key) {
  const text = String(src ?? "");
  const fences = fenceRanges(text);
  const marker = `_${key}:`;
  const out = [];
  let i = 0;
  for (; ; ) {
    const open = text.indexOf("<!--", i);
    if (open < 0) break;
    const close = text.indexOf("-->", open + 4);
    if (close < 0) break;
    i = close + 3;
    if (inFence(open, fences)) continue;
    const inner = text.slice(open + 4, close);
    const k = inner.indexOf(marker);
    if (k >= 0 && isBlank(inner.slice(0, k))) out.push(inner.slice(k + marker.length));
  }
  return out;
}
function parseSlideTags(slideSrc) {
  const include = /* @__PURE__ */ new Set();
  const exclude = /* @__PURE__ */ new Set();
  const found = findDirectiveComment(slideSrc, "lens");
  if (found) {
    for (const tok of found.body.split(/\s+/).filter(Boolean)) {
      if (tok.startsWith("-")) exclude.add(tok.slice(1));
      else if (tok.startsWith("+")) include.add(tok.slice(1));
      else include.add(tok);
    }
  }
  return { include, exclude };
}
function taggedLensIds(slides) {
  const ids = /* @__PURE__ */ new Set();
  for (const s of slides) {
    const { include, exclude } = parseSlideTags(s);
    for (const id of include) ids.add(id);
    for (const id of exclude) ids.add(id);
  }
  return ids;
}
function emitTokens(tags) {
  const inc = [...tags.include].sort();
  const exc = [...tags.exclude].sort().map((id) => `-${id}`);
  return [...inc, ...exc].join(" ");
}
function writeTags(slideSrc, tags) {
  const src = String(slideSrc ?? "");
  const tokens = emitTokens(tags);
  const comment = `<!-- _lens: ${tokens} -->`;
  const existing = findDirectiveComment(src, "lens");
  if (existing) {
    let end = existing.end;
    if (!tokens && src[end] === "\n") end += 1;
    return src.slice(0, existing.start) + (tokens ? comment : "") + src.slice(end);
  }
  if (!tokens) return src;
  const cls = findDirectiveComment(src, "class");
  if (cls) return `${src.slice(0, cls.end)}
${comment}${src.slice(cls.end)}`;
  return `${comment}
${src}`;
}
function applyTag(slideSrc, lensId, member, base) {
  const tags = parseSlideTags(slideSrc);
  if (base === "all") {
    tags.include.delete(lensId);
    if (member) tags.exclude.delete(lensId);
    else tags.exclude.add(lensId);
  } else {
    tags.exclude.delete(lensId);
    if (member) tags.include.add(lensId);
    else tags.include.delete(lensId);
  }
  return writeTags(slideSrc, tags);
}

// docs/src/lib/lente/types.ts
var FULL_LENS_ID = "full";

// docs/src/lib/lente/project.ts
function memberOf(slideSrc, lens2) {
  if (lens2.id === FULL_LENS_ID) return true;
  const t = parseSlideTags(slideSrc);
  return lens2.base === "all" ? !t.exclude.has(lens2.id) : t.include.has(lens2.id);
}
function pairsOf(slides) {
  return (Array.isArray(slides) ? slides : []).map((slide, index) => ({ slide, index })).filter((p) => typeof p.slide === "string");
}
function lensPairs(slides, reg, lensId) {
  const all = pairsOf(slides);
  const lens2 = reg.lenses.find((l) => l.id === lensId);
  if (!lens2 || lens2.id === FULL_LENS_ID) return all;
  const members = all.filter((p) => memberOf(p.slide, lens2));
  return lens2.single ? members.slice(0, 1) : members;
}
function lensSlides(slides, reg, lensId) {
  return lensPairs(slides, reg, lensId).map((p) => p.slide);
}
function lensIndices(slides, reg, lensId) {
  return lensPairs(slides, reg, lensId).map((p) => p.index);
}
function approvalHash(slides, reg, lensId) {
  const pairs = lensPairs(slides, reg, lensId);
  const lens2 = reg.lenses.find((l) => l.id === lensId);
  const base = lens2?.base ?? "none";
  const body = pairs.map((p) => `${p.index} ${p.slide}`).join("\n");
  return `sha256:${sha256Hex(`${lensId}
${base}
${body}`)}`;
}
function lensEligibility(slides, reg, lensId) {
  const lens2 = reg.lenses.find((l) => l.id === lensId);
  if (!lens2) return { status: "unavailable", reason: "unknown" };
  if (lens2.id === FULL_LENS_ID) return { status: "ok", pairs: pairsOf(slides) };
  if (lens2.hidden) return { status: "unavailable", reason: "hidden" };
  if (!lens2.approved) return { status: "unavailable", reason: "unapproved" };
  const pairs = lensPairs(slides, reg, lensId);
  if (pairs.length === 0) return { status: "unavailable", reason: "empty" };
  if (lens2.approved !== approvalHash(slides, reg, lensId)) return { status: "unavailable", reason: "drifted" };
  return { status: "ok", pairs };
}
function readerLenses(slides, reg) {
  return reg.lenses.filter((l) => l.id === FULL_LENS_ID || lensEligibility(slides, reg, l.id).status === "ok");
}

// docs/src/lib/lente/registry.ts
var DEFAULT_SCALAR_RE = /^\s*lens-default:(.*)$/;
var DEFAULTS_OFF_RE = /^\s*lens-defaults:\s*off\s*$/;
var HEADER_RE = /^\s*lenses:\s*$/;
var CHILD_RE = /^(\s+)([A-Za-z0-9_-]+):\s*\{(.*)\}\s*$/;
var INDENTED_RE = /^\s+\S/;
function stripQuotes(v) {
  const t = v.trim();
  if (t.length >= 2 && t.startsWith('"') && t.endsWith('"')) {
    try {
      return JSON.parse(t);
    } catch {
      return t.slice(1, -1);
    }
  }
  if (t.length >= 2 && t.startsWith("'") && t.endsWith("'")) {
    return t.slice(1, -1).replace(/\\(['\\])/g, "$1");
  }
  return t;
}
function splitTopLevel(body) {
  const out = [];
  let depth = 0;
  let quote = "";
  let cur = "";
  for (let i = 0; i < body.length; i++) {
    const ch = body[i];
    if (quote) {
      cur += ch;
      if (ch === "\\") {
        cur += body[++i] ?? "";
        continue;
      }
      if (ch === quote) quote = "";
      continue;
    }
    if (ch === '"' || ch === "'") {
      quote = ch;
      cur += ch;
      continue;
    }
    if (ch === "{" || ch === "[") depth++;
    if (ch === "}" || ch === "]") depth--;
    if (ch === "," && depth === 0) {
      out.push(cur);
      cur = "";
      continue;
    }
    cur += ch;
  }
  if (cur.trim()) out.push(cur);
  return out;
}
function parseInlineMap(id, body) {
  const def = { id };
  let drop = false;
  for (const pair of splitTopLevel(body)) {
    const idx = pair.indexOf(":");
    if (idx < 0) continue;
    const key = pair.slice(0, idx).trim();
    const raw = pair.slice(idx + 1).trim();
    switch (key) {
      case "label":
        def.label = stripQuotes(raw);
        break;
      case "base":
        def.base = raw === "all" ? "all" : "none";
        break;
      case "single":
        def.single = raw === "true";
        break;
      case "hidden":
        def.hidden = raw === "true";
        break;
      case "order": {
        const n = Number(raw);
        if (Number.isFinite(n)) def.order = n;
        break;
      }
      case "approved":
        def.approved = stripQuotes(raw);
        break;
      case "drop":
        drop = raw === "true";
        break;
      default:
        break;
    }
  }
  return { def, drop };
}
function parseEntries(lines) {
  const entries = [];
  let inBlock = false;
  for (const line of lines) {
    if (HEADER_RE.test(line)) {
      inBlock = true;
      continue;
    }
    if (!inBlock) continue;
    if (!INDENTED_RE.test(line)) {
      if (line.trim() === "") continue;
      break;
    }
    const m = CHILD_RE.exec(line);
    if (m) entries.push(parseInlineMap(m[2], m[3]));
  }
  return entries;
}
function completeDef(id, p) {
  const base = p.base === "all" ? "all" : "none";
  return {
    id,
    label: p.label ?? id,
    base,
    ...p.single ? { single: true } : {},
    ...p.hidden ? { hidden: true } : {},
    ...p.order != null ? { order: p.order } : {},
    ...p.approved ? { approved: p.approved } : {}
  };
}
var FULL_DEF = { id: FULL_LENS_ID, label: "Full deck", base: "all" };
function parseLensRegistry(frontMatter, workspace) {
  const lines = String(frontMatter ?? "").split(/\r?\n/);
  const defaultsOff = lines.some((l) => DEFAULTS_OFF_RE.test(l));
  const defaultLine = lines.map((l) => DEFAULT_SCALAR_RE.exec(l)).find(Boolean);
  const deckDefault = defaultLine ? stripQuotes(defaultLine[1].trim()) : void 0;
  const merged = /* @__PURE__ */ new Map();
  if (!defaultsOff && workspace) {
    for (const d of workspace.lenses) if (d.id !== FULL_LENS_ID) merged.set(d.id, { ...d });
  }
  for (const { def: def2, drop } of parseEntries(lines)) {
    if (def2.id === FULL_LENS_ID) continue;
    if (drop) {
      merged.delete(def2.id);
      continue;
    }
    merged.set(def2.id, { ...merged.get(def2.id), ...def2 });
  }
  const defs = [...merged.entries()].map(([id, p]) => completeDef(id, p));
  defs.sort((a, b) => (a.order ?? Number.POSITIVE_INFINITY) - (b.order ?? Number.POSITIVE_INFINITY));
  const lenses = [FULL_DEF, ...defs];
  const wantDefault = deckDefault ?? (!defaultsOff ? workspace?.default : void 0) ?? FULL_LENS_ID;
  const def = lenses.some((l) => l.id === wantDefault) ? wantDefault : FULL_LENS_ID;
  return { lenses, default: def };
}
function emitInline(d) {
  const parts = [`label: ${JSON.stringify(d.label)}`, `base: ${d.base}`];
  if (d.single) parts.push("single: true");
  if (d.hidden) parts.push("hidden: true");
  if (d.order != null) parts.push(`order: ${d.order}`);
  if (d.approved) parts.push(`approved: ${JSON.stringify(d.approved)}`);
  return `  ${d.id}: { ${parts.join(", ")} }`;
}
function emitRegistry(reg) {
  const body = reg.lenses.filter((l) => l.id !== FULL_LENS_ID).map(emitInline);
  return body.length ? `lenses:
${body.join("\n")}` : "";
}
function isPristineInherited(lens2, def) {
  return !!def && !lens2.approved && lens2.base === def.base && lens2.label === def.label && !!lens2.single === !!def.single && !!lens2.hidden === !!def.hidden && (lens2.order ?? null) === (def.order ?? null);
}
function emitInlineDelta(d, wsDef) {
  const parts = [`label: ${JSON.stringify(d.label)}`, `base: ${d.base}`];
  if (d.single) parts.push("single: true");
  else if (wsDef?.single) parts.push("single: false");
  if (d.hidden) parts.push("hidden: true");
  else if (wsDef?.hidden) parts.push("hidden: false");
  if (d.order != null) parts.push(`order: ${d.order}`);
  if (d.approved) parts.push(`approved: ${JSON.stringify(d.approved)}`);
  return `  ${d.id}: { ${parts.join(", ")} }`;
}
function emitRegistryDelta(reg, workspace, materialize) {
  const defs = new Map(workspace.lenses.filter((l) => l.id !== FULL_LENS_ID).map((l) => [l.id, l]));
  const lines = [];
  const present = /* @__PURE__ */ new Set();
  for (const lens2 of reg.lenses) {
    if (lens2.id === FULL_LENS_ID) continue;
    present.add(lens2.id);
    if (isPristineInherited(lens2, defs.get(lens2.id)) && !materialize?.has(lens2.id)) continue;
    lines.push(emitInlineDelta(lens2, defs.get(lens2.id)));
  }
  for (const id of defs.keys()) if (!present.has(id)) lines.push(`  ${id}: { drop: true }`);
  return lines.length ? `lenses:
${lines.join("\n")}` : "";
}
function stripRegistryLines(lines) {
  const out = [];
  let skipping = false;
  for (const line of lines) {
    if (HEADER_RE.test(line)) {
      skipping = true;
      continue;
    }
    if (skipping) {
      if (INDENTED_RE.test(line)) continue;
      if (line.trim() === "") continue;
      skipping = false;
    }
    if (DEFAULT_SCALAR_RE.test(line) || DEFAULTS_OFF_RE.test(line)) continue;
    out.push(line);
  }
  return out;
}
function upsertLensRegistry(frontMatter, reg, workspace, materialize) {
  const srcLines = String(frontMatter ?? "").split(/\r?\n/);
  const kept = stripRegistryLines(srcLines);
  while (kept.length && kept[kept.length - 1].trim() === "") kept.pop();
  const tail = [];
  const wsDefault = workspace?.default && workspace.default !== FULL_LENS_ID ? workspace.default : void 0;
  if (reg.default && reg.default !== (wsDefault ?? FULL_LENS_ID)) tail.push(`lens-default: ${reg.default}`);
  let block = workspace ? emitRegistryDelta(reg, workspace, materialize) : emitRegistry(reg);
  const present = new Set(reg.lenses.map((l) => l.id));
  for (const id of sourceDropIds(srcLines)) {
    if (present.has(id)) continue;
    const line = `  ${id}: { drop: true }`;
    if (block.includes(line)) continue;
    block = block ? `${block}
${line}` : `lenses:
${line}`;
  }
  if (block) tail.push(block);
  return [...kept, ...tail].join("\n");
}
function sourceDropIds(lines) {
  const ids = [];
  for (const { def, drop } of parseEntries(lines)) if (drop && def.id !== FULL_LENS_ID && !ids.includes(def.id)) ids.push(def.id);
  return ids;
}

// docs/src/lib/lente/builder.ts
function lens(slides) {
  let reg = parseLensRegistry("");
  let lensId = FULL_LENS_ID;
  const b = {
    registry(r) {
      reg = typeof r === "string" ? parseLensRegistry(r) : r;
      return b;
    },
    pick(id) {
      lensId = id;
      return b;
    },
    project() {
      return lensEligibility(slides, reg, lensId);
    },
    slides() {
      return lensSlides(slides, reg, lensId);
    },
    pairs() {
      return lensPairs(slides, reg, lensId);
    },
    indices() {
      return lensIndices(slides, reg, lensId);
    },
    pickable() {
      return readerLenses(slides, reg);
    },
    hash() {
      return approvalHash(slides, reg, lensId);
    }
  };
  return b;
}

// docs/src/lib/lente/suggest.ts
var NAME_RE = /^[A-Za-z0-9-]+$/;
function slideClass(src) {
  const found = findDirectiveComment(src, "class");
  if (!found) return "text";
  const first = found.body.trim().split(/[ \t]+/)[0] ?? "";
  return NAME_RE.test(first) ? first : "text";
}
var SUGGESTERS = {
  // brief (base:none) — the bottom line. Bookend frame + assertions + headline metrics; NO detail.
  brief(slides, catalog) {
    const out = [];
    slides.forEach((slide, index) => {
      const name = slideClass(slide);
      const info = catalog.get(name);
      if (!info) return;
      let reason = "";
      if (info.function === "anchor" && info.form === "bookend") reason = `${name} \u2192 title/closing frame \u2192 Brief`;
      else if (info.function === "statement" && info.bucket !== "connect") reason = `${name} \u2192 key assertion \u2192 Brief`;
      else if (name === "kpi" || name === "stats") reason = `${name} \u2192 headline metric \u2192 Brief`;
      if (reason) out.push({ index, lensId: "brief", member: true, reason });
    });
    return out;
  },
  // ask (base:none, single) — exactly ONE slide, or NONE (never a low-confidence guess).
  ask(slides) {
    let pick = -1;
    let reason = "";
    slides.forEach((slide, index) => {
      if (slideClass(slide) === "closing") {
        pick = index;
        reason = "last closing slide \u2192 the CTA \u2192 Ask";
      }
    });
    if (pick < 0) {
      const decision = slides.findIndex((s) => slideClass(s) === "decision");
      if (decision >= 0) {
        pick = decision;
        reason = "decision component \u2192 Ask";
      }
    }
    if (pick < 0) {
      for (const metric of ["big-number", "kpi", "stats"]) {
        const i = slides.findIndex((s) => slideClass(s) === metric);
        if (i >= 0) {
          pick = i;
          reason = `${metric} \u2192 headline metric \u2192 Ask`;
          break;
        }
      }
    }
    return pick >= 0 ? [{ index: pick, lensId: "ask", member: true, reason }] : [];
  },
  // story (base:none) — the throughline: anchors (incl. dividers), the journey, and the problem setup.
  story(slides, catalog) {
    const firstNonAnchor = slides.findIndex((s) => (catalog.get(slideClass(s))?.function ?? "anchor") !== "anchor");
    const out = [];
    slides.forEach((slide, index) => {
      const name = slideClass(slide);
      const fn = catalog.get(name)?.function;
      let reason = "";
      if (fn === "anchor") reason = `${name} \u2192 chapter frame \u2192 Story`;
      else if (fn === "progression") reason = `${name} \u2192 the journey \u2192 Story`;
      else if (index === firstNonAnchor) reason = "first non-anchor slide \u2192 the problem setup \u2192 Story";
      if (reason) out.push({ index, lensId: "story", member: true, reason });
    });
    return out;
  },
  // evidence (base:all) — keep everything substantive; EXCLUDE decoration, logistics, and dividers.
  evidence(slides, catalog) {
    const out = [];
    slides.forEach((slide, index) => {
      const name = slideClass(slide);
      const info = catalog.get(name);
      if (!info) return;
      const drop = info.function === "imagery" || info.function === "statement" && info.bucket === "connect" || info.function === "anchor" && info.form === "divider";
      if (drop) out.push({ index, lensId: "evidence", member: false, reason: `${name} \u2192 not substantive \u2192 dropped from Evidence` });
    });
    return out;
  }
};
function suggestMembership(slides, reg, catalog) {
  const src = Array.isArray(slides) ? slides : [];
  const out = [];
  for (const lens2 of reg.lenses) {
    if (lens2.id === FULL_LENS_ID) continue;
    const rule = SUGGESTERS[lens2.id];
    if (rule) out.push(...rule(src, catalog));
  }
  return out;
}
function catalogFromComponents(components) {
  return new Map(components.map((c) => [c.name, { bucket: c.bucket, function: c.function, form: c.form }]));
}

// docs/src/lib/lente/validate.ts
function unknownLensTokens(src, reg) {
  const known = new Set(reg.lenses.map((l) => l.id));
  const seen = /* @__PURE__ */ new Set();
  for (const body of allDirectiveBodies(src, "lens")) {
    for (const raw of body.split(/\s+/).filter(Boolean)) {
      const id = raw.replace(/^[+-]/, "");
      if (id && !known.has(id) && !seen.has(id)) seen.add(id);
    }
  }
  return [...seen];
}
function validateRegistry(slides, reg) {
  const out = [];
  const src = Array.isArray(slides) ? slides : [];
  if (reg.default !== FULL_LENS_ID) {
    const elig = lensEligibility(src, reg, reg.default);
    if (elig.status !== "ok") {
      out.push({
        level: "warning",
        code: "default-unavailable",
        lensId: reg.default,
        message: `lens-default "${reg.default}" is ${elig.reason} \u2014 readers will land on Full instead`
      });
    }
  }
  const known = new Set(reg.lenses.map((l) => l.id));
  src.forEach((slide, index) => {
    const t = parseSlideTags(slide);
    for (const id of t.include) {
      if (t.exclude.has(id)) {
        out.push({ level: "warning", code: "tag-contradiction", slide: index, lensId: id, message: `slide ${index + 1}: both +${id} and -${id} \u2014 the wrong-polarity token is dead` });
      }
    }
    for (const id of [...t.include, ...t.exclude]) {
      if (!known.has(id)) out.push({ level: "warning", code: "orphan-tag", slide: index, lensId: id, message: `slide ${index + 1}: _lens token "${id}" names no registered lens` });
    }
  });
  return out;
}
function rebaseLensTags(slides, reg, lensId, from, to) {
  const known = reg.lenses.some((l) => l.id === lensId);
  if (from === to || !known) return [...slides];
  return (Array.isArray(slides) ? slides : []).map((slide) => {
    const t = parseSlideTags(slide);
    const member = from === "all" ? !t.exclude.has(lensId) : t.include.has(lensId);
    return applyTag(slide, lensId, member, to);
  });
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  FULL_LENS_ID,
  applyTag,
  approvalHash,
  catalogFromComponents,
  emitRegistry,
  emitRegistryDelta,
  isPristineInherited,
  lens,
  lensEligibility,
  lensIndices,
  lensPairs,
  lensSlides,
  parseLensRegistry,
  parseSlideTags,
  readerLenses,
  rebaseLensTags,
  sha256Hex,
  suggestMembership,
  taggedLensIds,
  unknownLensTokens,
  upsertLensRegistry,
  validateRegistry
});
