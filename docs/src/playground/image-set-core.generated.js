/* Auto-generated by tools/build-image-set-core.js — DO NOT EDIT.
   Source: lib/export/image-set.js. Rebuild: npm run image-set-core:build */
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// lib/export/image-set.js
var require_image_set = __commonJS({
  "lib/export/image-set.js"(exports, module) {
    var IMAGE_FORMATS2 = ["png", "jpeg", "webp"];
    var FORMAT_META2 = {
      png: { ext: "png", mime: "image/png", lossy: false },
      jpeg: { ext: "jpeg", mime: "image/jpeg", lossy: true },
      webp: { ext: "webp", mime: "image/webp", lossy: true }
    };
    var SIZE_PRESETS2 = ["max", "2x", "1x", "half"];
    var RASTER_MAX_EDGE2 = 3840;
    var COLOR_MODES2 = ["inherit", "light", "dark", "print"];
    var SVG_BACKGROUNDS2 = ["inherit", "light", "dark", "print"];
    var SVG_BACKGROUND_FILL2 = { inherit: null, light: "#ffffff", dark: "#111317", print: "#ffffff" };
    var SVG_LOOK_MODE2 = { inherit: null, light: "light", dark: "dark", print: "print" };
    var DEFAULTS2 = Object.freeze({
      format: "png",
      size: "max",
      quality: 92,
      // JPEG/WebP encoder quality (1–100); ignored for PNG
      thumbnails: true,
      thumbWidth: 480,
      // px wide; thumbnail height follows the slide aspect
      extractSvg: true,
      // charts + Mermaid/diagram SVGs as standalone files
      mode: "inherit",
      // export color mode; 'inherit' = the deck's own / palette-resolved
      svgBackground: "inherit"
      // the extracted SVG's look; 'inherit' = follow the slides, no canvas
    });
    function normalizeImageSetOptions2(raw) {
      const o = raw || {};
      const format = IMAGE_FORMATS2.includes(o.format) ? o.format : DEFAULTS2.format;
      const size = SIZE_PRESETS2.includes(o.size) ? o.size : DEFAULTS2.size;
      let quality = Number(o.quality);
      if (!Number.isFinite(quality)) quality = DEFAULTS2.quality;
      quality = Math.min(100, Math.max(1, Math.round(quality)));
      let thumbWidth = Number(o.thumbWidth);
      if (!Number.isFinite(thumbWidth) || thumbWidth <= 0) thumbWidth = DEFAULTS2.thumbWidth;
      thumbWidth = Math.min(2e3, Math.max(48, Math.round(thumbWidth)));
      return {
        format,
        size,
        quality,
        thumbnails: o.thumbnails === void 0 ? DEFAULTS2.thumbnails : !!o.thumbnails,
        thumbWidth,
        extractSvg: o.extractSvg === void 0 ? DEFAULTS2.extractSvg : !!o.extractSvg,
        mode: COLOR_MODES2.includes(o.mode) ? o.mode : DEFAULTS2.mode,
        svgBackground: SVG_BACKGROUNDS2.includes(o.svgBackground) ? o.svgBackground : DEFAULTS2.svgBackground
      };
    }
    function svgBackgroundFill2(svgBackground) {
      return Object.hasOwn(SVG_BACKGROUND_FILL2, svgBackground) ? SVG_BACKGROUND_FILL2[svgBackground] : SVG_BACKGROUND_FILL2[DEFAULTS2.svgBackground];
    }
    function svgLookMode2(svgBackground) {
      return Object.hasOwn(SVG_LOOK_MODE2, svgBackground) ? SVG_LOOK_MODE2[svgBackground] : SVG_LOOK_MODE2[DEFAULTS2.svgBackground];
    }
    function resolveRasterScale2(size, slideW, slideH) {
      const longEdge = Math.max(Number(slideW) || 1280, Number(slideH) || 720);
      const cap = RASTER_MAX_EDGE2 / longEdge;
      switch (size) {
        case "2x":
          return Math.min(2, cap);
        case "1x":
          return Math.min(1, cap);
        case "half":
          return Math.min(0.5, cap);
        // 'max' (and any unknown value) → fidelity-first, capped at the long-edge budget.
        default:
          return Math.max(1, Math.min(2, Math.floor(cap)));
      }
    }
    function resolveThumbScale2(thumbWidth, slideW) {
      const w = Number(slideW) || 1280;
      return Math.min(1, (Number(thumbWidth) || DEFAULTS2.thumbWidth) / w);
    }
    var PHYSICAL_LONG_EDGE_IN2 = 13.333;
    function orientationOf2(w, h) {
      const a = Number(w) || 0;
      const b = Number(h) || 0;
      if (a > b) return "landscape";
      if (b > a) return "portrait";
      return "square";
    }
    function physicalSize2(w, h) {
      const a = Math.max(Number(w) || 1, 1);
      const b = Math.max(Number(h) || 1, 1);
      const longest = Math.max(a, b);
      const round3 = (n) => Math.round(n / longest * PHYSICAL_LONG_EDGE_IN2 * 1e3) / 1e3;
      return { width: round3(a), height: round3(b), unit: "in" };
    }
    function dpiFor2(pixelW, pixelH) {
      const px = Math.max(Number(pixelW) || 0, Number(pixelH) || 0);
      return Math.round(px / PHYSICAL_LONG_EDGE_IN2);
    }
    function dataByteLength2(data) {
      if (data == null) return 0;
      if (typeof data === "string") return new TextEncoder().encode(data).length;
      if (typeof data.byteLength === "number") return data.byteLength;
      if (typeof data.size === "number") return data.size;
      return 0;
    }
    var _crcTable = null;
    function crc32(bytes) {
      if (!_crcTable) {
        _crcTable = new Uint32Array(256);
        for (let n = 0; n < 256; n++) {
          let c = n;
          for (let k = 0; k < 8; k++) c = c & 1 ? 3988292384 ^ c >>> 1 : c >>> 1;
          _crcTable[n] = c >>> 0;
        }
      }
      let crc = 4294967295;
      for (let i = 0; i < bytes.length; i++) crc = _crcTable[(crc ^ bytes[i]) & 255] ^ crc >>> 8;
      return (crc ^ 4294967295) >>> 0;
    }
    var DPI_PER_METER = 1 / 0.0254;
    function u8(...arrs) {
      const len = arrs.reduce((n, a) => n + a.length, 0);
      const out = new Uint8Array(len);
      let o = 0;
      for (const a of arrs) {
        out.set(a, o);
        o += a.length;
      }
      return out;
    }
    function be32(n) {
      const v = n >>> 0;
      return Uint8Array.of(v >>> 24 & 255, v >>> 16 & 255, v >>> 8 & 255, v & 255);
    }
    function embedPngDpi(bytes, dpi) {
      const SIG = [137, 80, 78, 71, 13, 10, 26, 10];
      if (bytes.length < 8 + 25) return bytes;
      for (let i = 0; i < 8; i++) if (bytes[i] !== SIG[i]) return bytes;
      const ihdrType = String.fromCharCode(bytes[12], bytes[13], bytes[14], bytes[15]);
      if (ihdrType !== "IHDR") return bytes;
      const ihdrEnd = 8 + 25;
      const ppm = Math.round(dpi * DPI_PER_METER);
      const data = u8(be32(ppm), be32(ppm), Uint8Array.of(1));
      const typeAndData = u8(Uint8Array.of(112, 72, 89, 115), data);
      const chunk = u8(be32(9), typeAndData, be32(crc32(typeAndData)));
      return u8(bytes.subarray(0, ihdrEnd), chunk, bytes.subarray(ihdrEnd));
    }
    function embedJpegDpi(bytes, dpi) {
      if (bytes.length < 20 || bytes[0] !== 255 || bytes[1] !== 216) return bytes;
      if (bytes[2] !== 255 || bytes[3] !== 224) return bytes;
      const isJfif = bytes[6] === 74 && bytes[7] === 70 && bytes[8] === 73 && bytes[9] === 70 && bytes[10] === 0;
      if (!isJfif) return bytes;
      const d = Math.min(65535, Math.max(1, Math.round(dpi)));
      const out = bytes.slice();
      out[13] = 1;
      out[14] = d >>> 8 & 255;
      out[15] = d & 255;
      out[16] = d >>> 8 & 255;
      out[17] = d & 255;
      return out;
    }
    function embedRasterDpi2(bytes, format, dpi) {
      const b = bytes instanceof Uint8Array ? bytes : new Uint8Array(bytes || []);
      const d = Number(dpi);
      if (!Number.isFinite(d) || d <= 0) return bytes;
      try {
        if (format === "png") return embedPngDpi(b, d);
        if (format === "jpeg") return embedJpegDpi(b, d);
      } catch (_e) {
      }
      return bytes;
    }
    function padWidth2(count) {
      return Math.max(2, String(Math.max(1, count)).length);
    }
    function deckSlug2(name) {
      const s = String(name || "deck").trim().replace(/[^\w.-]+/g, "-").replace(/^[-.]+|[-.]+$/g, "").slice(0, 60);
      if (!s || /^\.+$/.test(s)) return "deck";
      if (/^(con|prn|aux|nul|com[1-9]|lpt[1-9])$/i.test(s)) return `deck-${s}`;
      return s;
    }
    function slideEntryName2(slug, index, count, ext) {
      return `slides/${slug}-${String(index + 1).padStart(padWidth2(count), "0")}.${ext}`;
    }
    function thumbEntryName2(slug, index, count, ext) {
      return `thumbnails/${slug}-${String(index + 1).padStart(padWidth2(count), "0")}.${ext}`;
    }
    function assetEntryName2(slug, slide, index, count) {
      const s = String(slide).padStart(padWidth2(count), "0");
      const i = String(index).padStart(2, "0");
      return `assets/${slug}-s${s}-c${i}.svg`;
    }
    function buildImageSetManifest2(p) {
      const opts = p.options || DEFAULTS2;
      const meta = FORMAT_META2[opts.format] || FORMAT_META2.png;
      const geom = p.geom || { w: 1280, h: 720 };
      const scale = Number(p.scale) || 1;
      const pixel = { width: Math.round(geom.w * scale), height: Math.round(geom.h * scale), scale };
      const thumbs = p.thumbnails || [];
      const thumbFor = (slide) => thumbs.find((t) => t.slide === slide);
      const titles = p.slideTitles || [];
      const out = {
        kind: "lattice-image-set",
        version: 2,
        generator: p.generator || "lattice",
        deck: p.slug,
        title: p.title || null,
        palette: p.palette || null,
        engine: { name: "lattice", version: p.engineVersion || null },
        // Provenance — a stamp of WHEN the set was made. Omitted when the surface doesn't pass
        // one (keeps the manifest byte-reproducible for callers that want that).
        ...p.createdAt ? { createdAt: p.createdAt } : {},
        format: opts.format,
        mime: meta.mime,
        lossy: meta.lossy,
        quality: meta.lossy ? opts.quality : null,
        colorMode: opts.mode || DEFAULTS2.mode,
        svgBackground: opts.svgBackground || DEFAULTS2.svgBackground,
        orientation: orientationOf2(geom.w, geom.h),
        slide: { width: geom.w, height: geom.h },
        pixel,
        physical: physicalSize2(geom.w, geom.h),
        dpi: dpiFor2(pixel.width, pixel.height),
        counts: {
          slides: (p.slides || []).length,
          thumbnails: thumbs.length,
          assets: (p.assets || []).length
        },
        slides: (p.slides || []).map((s) => {
          const t = thumbFor(s.slide);
          return {
            slide: s.slide,
            title: titles[s.slide - 1] || null,
            image: s.name,
            bytes: s.bytes ?? null,
            thumbnail: t?.name || null,
            thumbnailBytes: t?.bytes ?? null
          };
        }),
        assets: (p.assets || []).map((a) => ({
          slide: a.slide,
          kind: a.kind || "svg",
          chartType: a.chartType || null,
          file: a.name,
          bytes: a.bytes ?? null
        }))
      };
      if (opts.thumbnails && thumbs.length) {
        const ts = resolveThumbScale2(opts.thumbWidth, geom.w);
        out.thumbnail = { width: Math.round(geom.w * ts), height: Math.round(geom.h * ts) };
      }
      return out;
    }
    function assembleImageSetPlan2(p) {
      const options = normalizeImageSetOptions2(p.options);
      const meta = FORMAT_META2[options.format];
      const slug = deckSlug2(p.name);
      const images = Array.isArray(p.images) ? p.images : [];
      const thumbs = Array.isArray(p.thumbs) ? p.thumbs : [];
      const svgs = Array.isArray(p.svgs) ? p.svgs : [];
      const count = images.length;
      if (!count) throw new Error("assembleImageSetPlan: no slide images to pack");
      const files = [];
      const slideEntries = [];
      const thumbEntries = [];
      const assetEntries = [];
      images.forEach((data, i) => {
        const name = slideEntryName2(slug, i, count, meta.ext);
        files.push({ path: name, data });
        slideEntries.push({ name, slide: i + 1, bytes: dataByteLength2(data) });
      });
      if (options.thumbnails) {
        thumbs.forEach((data, i) => {
          if (!data) return;
          const name = thumbEntryName2(slug, i, count, meta.ext);
          files.push({ path: name, data });
          thumbEntries.push({ name, slide: i + 1, bytes: dataByteLength2(data) });
        });
      }
      if (options.extractSvg) {
        const perSlide = /* @__PURE__ */ new Map();
        for (const a of svgs) {
          if (!a?.svg) continue;
          const n = perSlide.get(a.slide) || 0;
          perSlide.set(a.slide, n + 1);
          const name = assetEntryName2(slug, a.slide, n, count);
          files.push({ path: name, data: a.svg });
          assetEntries.push({ name, slide: a.slide, kind: a.kind || "svg", chartType: a.chartType || null, bytes: dataByteLength2(a.svg) });
        }
      }
      const manifest = buildImageSetManifest2({
        slug,
        options,
        geom: p.geom || { w: 1280, h: 720 },
        scale: p.scale,
        slides: slideEntries,
        thumbnails: thumbEntries,
        assets: assetEntries,
        slideTitles: p.slideTitles,
        title: p.title,
        palette: p.palette,
        engineVersion: p.engineVersion,
        createdAt: p.createdAt,
        generator: p.generator
      });
      files.push({ path: "manifest.json", data: JSON.stringify(manifest, null, 2) });
      return { files, manifest, slug };
    }
    function addPlanToZip2(zip, plan) {
      const root = zip.folder(plan.slug);
      for (const f of plan.files) root.file(f.path, f.data);
      return zip;
    }
    module.exports = {
      IMAGE_FORMATS: IMAGE_FORMATS2,
      FORMAT_META: FORMAT_META2,
      SIZE_PRESETS: SIZE_PRESETS2,
      RASTER_MAX_EDGE: RASTER_MAX_EDGE2,
      COLOR_MODES: COLOR_MODES2,
      SVG_BACKGROUNDS: SVG_BACKGROUNDS2,
      SVG_BACKGROUND_FILL: SVG_BACKGROUND_FILL2,
      SVG_LOOK_MODE: SVG_LOOK_MODE2,
      DEFAULTS: DEFAULTS2,
      normalizeImageSetOptions: normalizeImageSetOptions2,
      resolveRasterScale: resolveRasterScale2,
      resolveThumbScale: resolveThumbScale2,
      svgBackgroundFill: svgBackgroundFill2,
      svgLookMode: svgLookMode2,
      PHYSICAL_LONG_EDGE_IN: PHYSICAL_LONG_EDGE_IN2,
      orientationOf: orientationOf2,
      physicalSize: physicalSize2,
      dpiFor: dpiFor2,
      dataByteLength: dataByteLength2,
      embedRasterDpi: embedRasterDpi2,
      padWidth: padWidth2,
      deckSlug: deckSlug2,
      slideEntryName: slideEntryName2,
      thumbEntryName: thumbEntryName2,
      assetEntryName: assetEntryName2,
      buildImageSetManifest: buildImageSetManifest2,
      assembleImageSetPlan: assembleImageSetPlan2,
      addPlanToZip: addPlanToZip2
    };
  }
});

// lib/export/image-set-core.entry.js
var import_image_set = __toESM(require_image_set());
var {
  IMAGE_FORMATS,
  FORMAT_META,
  SIZE_PRESETS,
  RASTER_MAX_EDGE,
  COLOR_MODES,
  SVG_BACKGROUNDS,
  SVG_BACKGROUND_FILL,
  SVG_LOOK_MODE,
  DEFAULTS,
  normalizeImageSetOptions,
  resolveRasterScale,
  resolveThumbScale,
  svgBackgroundFill,
  svgLookMode,
  PHYSICAL_LONG_EDGE_IN,
  orientationOf,
  physicalSize,
  dpiFor,
  dataByteLength,
  embedRasterDpi,
  padWidth,
  deckSlug,
  slideEntryName,
  thumbEntryName,
  assetEntryName,
  buildImageSetManifest,
  assembleImageSetPlan,
  addPlanToZip
} = import_image_set.default;
export {
  COLOR_MODES,
  DEFAULTS,
  FORMAT_META,
  IMAGE_FORMATS,
  PHYSICAL_LONG_EDGE_IN,
  RASTER_MAX_EDGE,
  SIZE_PRESETS,
  SVG_BACKGROUNDS,
  SVG_BACKGROUND_FILL,
  SVG_LOOK_MODE,
  addPlanToZip,
  assembleImageSetPlan,
  assetEntryName,
  buildImageSetManifest,
  dataByteLength,
  deckSlug,
  dpiFor,
  embedRasterDpi,
  normalizeImageSetOptions,
  orientationOf,
  padWidth,
  physicalSize,
  resolveRasterScale,
  resolveThumbScale,
  slideEntryName,
  svgBackgroundFill,
  svgLookMode,
  thumbEntryName
};
