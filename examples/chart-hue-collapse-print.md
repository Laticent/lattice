---
marp: true
theme: indaco
color-mode: print
paginate: true
header: "Lattice · One hue in grayscale"
---

<!-- _class: title silent -->

`Chart color · the print channel`

# One hue is one texture

The print and a11y channels substitute **texture** for hue, so the two counts have to agree. With `funnel` and `timeline-list` down to one hue, the funnel's six band patterns collapse to one — six would claim six categories in grayscale while the color channel says one.

---

<!-- _class: funnel -->
<!-- _footer: "One texture · funnel" -->

## The funnel narrows; the width is the story.

- Visitors `12,000`
- Signups `4,800`
- Activated `2,160`
- Paid `864`
- Renewed `670`

---

<!-- _class: timeline-list -->
<!-- _footer: "One ink · timeline-list" -->

`Milestones · one ink`

## The spine orders the events; the dates name them.

1. `2025 Q1` Framework approved `decision`
   - Each dot's title sits directly under it.
2. `2025 Q2` First cohort live `live`
   - No dot is near enough to touch its neighbor.
3. `2025 Q3` Coverage extended `on-track`
   - So a hue per dot separated nothing.
4. `2025 Q4` Renewal opens `decision`
   - Spine, pills and dots now read as one figure.

---

<!-- _class: closing -->
<!-- _paginate: false -->
<!-- _footer: '' -->

## The hue count is the texture count, so both go to one.

`Chart color · the print channel`
