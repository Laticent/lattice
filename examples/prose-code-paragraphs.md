---
theme: indaco
paginate: true
header: "Prose with code"
---

<!-- _class: title -->

`Engine · prose-code.js`

# A sentence with one code span is body prose

The `:only-child` selector cannot see text, so this line used to render as a muted subtitle.

---

<!-- _class: topic -->

`Section 01 · The panel`

## Where the background goes

The `background:` shorthand clears every longhand at once.

---

<!-- _class: content -->

## A body slide keeps its sentence

The `--field-accent` slot names the color a finish draws in, and the pill stays a pill.

- The sentence reads at body weight
  - It was muted, italic and smaller, and its code chip lost the pill.
- Nothing about the author's source changed
  - The engine marks the paragraph and the stylesheet reads the mark.

---

<!-- _class: content -->

## A real subtitle still reads as one

`Four-stage processing pipeline, weekly cadence`

- A paragraph that is only a code span
  - It is still promoted to the italic subtitle, exactly as before.
- An eyebrow above the heading works the same way
  - The whole kicker is the code span, so the rule still matches it.

---

<!-- _class: authority-chain branching -->

## A list item follows the same rule, so every branch reads alike.

1. Statute
   - `15 U.S.C. §6501` COPPA, 1998
   - `16 C.F.R. Part 312` FTC implementing rule
   - `In re Epic Games · 2022` consent order

---

<!-- _class: closing -->

`#2308`

## One code span in a sentence is still a sentence.

A renderer that never runs the engine keeps the old behavior, so nothing gets worse.
