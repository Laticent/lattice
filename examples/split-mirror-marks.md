---
marp: true
theme: indaco
size: square
paginate: true
header: "Split marks under mirror"
footer: "split-panel mirror · the wayfinding marks change sides"
---

<!-- _class: title -->

# The marks change sides

`Auto-split · split-panel mirror`

`mirror` reverses the panel row, so the bottom-right corner the split page puts its wayfinding marks in stops being the supporting column and becomes the panel. Both marks had to be told.

---

<!-- _class: split-panel pullquote mirror -->

> The pointer moves with the row. So does its band.

`the forward pointer`

- Reserved where the pill is
  - The panel the pointer reaches gives up the band.
- Not on the other one
  - Reserving both grew the dark panel.
- The same band on every page
  - The last page has no pointer and holds it.

---

<!-- _class: split-panel pullquote mirror watermark -->

> Canvas ink is the wrong ink once the corner is the panel.

`the k-of-N rail`

- Navy on navy
  - The base panel measured 1.02 to 1 across light palettes.
- Accent underneath
  - The watermark panel measured 1.00 to 1 on all 33.
- The panel's own ink now
  - Worst cell across 33 palettes is 5.33 to 1.

---

<!-- _class: split-panel pullquote mirror cat-4 -->

> A tinted panel now takes the ink its contract guarantees.

`the tinted panels`

- Light fills read fine
  - Canvas ink cleared 3 to 1 on every palette.
- The contract is stronger
  - `--cat-on-fill` holds against any fill.
- Metric and steps untouched
  - Their panels are canvas, so they keep canvas ink.

---

<!-- _class: closing -->

## What a reader should see

Nothing printed through, on either panel, at any size — and a progress rail you can actually find. The page number in the same corner is still canvas ink on the panel (1.07 to 1 here); it sits on the unsplit slide too, so it is not this change's and is recorded rather than folded in.
