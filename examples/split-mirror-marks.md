---
marp: true
theme: indaco
size: square
paginate: true
header: "Split marks under mirror"
footer: "split-panel mirror · the wayfinding marks change sides"
---

<!-- _class: title -->

# The marks change sides

`Auto-split · split-panel`

A split page puts its wayfinding marks in the bottom-right corner. Three things decide what is in that corner: `mirror` reverses the row, `metric` inverts the panels, and a Form frame lifts both panels off it. The marks had to be told.

---

<!-- _class: split-panel pullquote mirror -->

> The pointer moves with the row. So does its band.

`the forward pointer`

- Reserved where the pill is
  - The panel the pointer reaches gives up the band.
- Not on the other one
  - Reserving both grew the dark panel.
- The same band on every page
  - The last page has no pointer and holds it.

---

<!-- _class: split-panel pullquote mirror watermark -->

> Canvas ink is the wrong ink once the corner is the panel.

`the k-of-N rail`

- Navy on navy
  - The mirrored panel ran 1.02 to 2.72 across the light palettes.
- Accent underneath
  - The watermark panel ran 1.00 to 2.61 on all 33.
- The field's own ink now
  - 627 cells swept, none under 3 to 1, floor 5.11.

---

<!-- _class: split-panel pullquote metric -->

> Two corners never had a mirror in them, and keying on one missed both.

`the corner, not the modifier`

- Metric inverts the panels
  - Its dark half is the right one, and the rail sat on it at 1.02 to 1.
- A Form frame insets them
  - The rail drops below both, onto canvas, where panel ink read 1.00 to 1.
- The rule asks what is in the corner
  - Not which modifier is on the slide.

---

<!-- _class: closing -->

## What a reader should see

Nothing printed through, on either panel, at any size — and a progress rail you can find. Two other marks in that corner read the same way unsplit, so they are recorded, not fixed here.
