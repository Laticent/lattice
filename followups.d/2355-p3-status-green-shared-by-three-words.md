---
origin: 2355
priority: P3
recorded: 2026-09-24
---

# on-track, done and live paint one green now that status paints the whole state

```text
  P3 · [no ticket] The pill vocabulary maps on-track, done and live to one tone (pass).
       why now   — on a state chart the status now tints the whole tile, so a legend listing
                   three words shows three identical chips; gantt already split `live` to info.
       where     — chart-family.css `.chart-status` table (the shared vocabulary) and every
                   member reading it; a vocabulary change is the owner's call.
       done when — a decision on whether `live` (and `done`) get their own tone family-wide.
       evidence  — the decision, and a render of the state-chart legend.
       verify    — tier 1.
```
