{
  "$schema": "../../manifest.schema.json",
  "name": "math",
  "function": "evidence",
  "bucket": "math",
  "form": "canvas",
  "substance": "prose",
  "adapt": {
    "mode": "reflow",
    "families": [
      "wide",
      "square",
      "tall",
      "strip"
    ],
    "priority": [
      "title",
      "equation",
      "annotations"
    ],
    "droppable": [
      "annotations"
    ]
  },
  "tags": [
    "formula",
    "assessment",
    "reference"
  ],
  "description": "Boardroom-quality math layouts for mathematicians, quants, ML researchers, physicists, statisticians, and economists. KaTeX-rendered equations with persona-appropriate surround.",
  "purpose": "Use when the slide IS the equation. KaTeX renders `$$…$$` as centered display blocks and `$…$` inline. Variants surround the math with the structure each persona expects: hero + legend (feature), step + justification (derivation), Definition/Theorem/Proof cards (theorem), side-by-side comparison (compare), equation + plot (canvas), matrix + properties (matrix), estimate ± uncertainty + interpretation (stats).",
  "variants": [
    "feature",
    "derivation",
    "theorem",
    "compare",
    "canvas",
    "matrix",
    "stats",
    "decompose"
  ],
  "slots": {
    "eyebrow": {
      "selector": "p:first-child > code",
      "required": false,
      "description": "Optional inline-code rubric above the heading (e.g. `Linear regression · OLS`). Authored as an inline-code paragraph, not a heading, so it stays lint-safe (no heading-order violation)."
    },
    "heading": {
      "selector": "h2",
      "required": true,
      "description": "One-sentence framing of what the math establishes."
    },
    "equation": {
      "selector": "p",
      "required": true,
      "description": "Display equation wrapped in `$$…$$`. KaTeX renders centered."
    },
    "legend": {
      "selector": "ul > li",
      "required": false,
      "description": "'where:' legend. Each li introduces an `$x$` symbol followed by its definition."
    }
  },
  "skeleton": "<!-- _class: math -->\n\n`Eyebrow · context`\n\n## One-sentence framing of what the equation establishes.\n\n$$ y = f(x) $$\n\n- $y$ — what we predict\n- $x$ — input variable\n- $f$ — the relation under study\n",
  "anatomyBlock": "math-canvas",
  "sample": "<!-- _class: math -->\n\n`Linear regression · OLS`\n\n## One equation, displayed; its symbols named below.\n\n$$ \\hat\\beta = (X^\\top X)^{-1} X^\\top y $$\n\n- $\\hat\\beta$ — OLS coefficient vector\n- $X$ — design matrix, $n \\times p$\n- $y$ — response vector, length $n$\n- $X^\\top X$ — Gram matrix, $p \\times p$, must be invertible\n",
  "stressDoc": {
    "summary": "Three steps — the longest chain.",
    "sample": "<!-- _class: math derivation -->\n\n`math · stress`\n\n## Three displayed steps is the longest chain.\n\n$$ \\hat\\beta = \\arg\\min_\\beta \\, \\lVert y - X\\beta \\rVert_2^2 $$\n\n$$ X^\\top X \\hat\\beta = X^\\top y $$\n\n$$ \\hat\\beta = (X^\\top X)^{-1} X^\\top y $$\n\n- Step one states the objective\n- Step two takes the gradient to zero\n- Step three is the ceiling — a fourth step means two slides\n"
  },
  "whenToUse": [
    {
      "title": "The equation IS the argument",
      "body": "When a single closed-form expression, identity, or estimator carries the slide. KaTeX renders it; Lattice gives it the room. For surrounding prose with one inline `$x$`, use content."
    },
    {
      "title": "Pick the variant from the persona",
      "body": "Quants reach for feature and stats. ML researchers reach for canvas (equation + plot). Pure mathematicians reach for theorem and derivation. Linear-algebra-heavy work reaches for matrix. The base layout works for everyone."
    },
    {
      "title": "Legend, not footnotes",
      "body": "The `where:` list under the equation defines every symbol introduced. The audience should never have to scroll back to remember what $\\hat\\beta$ or $X$ stands for."
    }
  ],
  "antiPatterns": [
    {
      "title": "Two display equations in the base layout",
      "body": "The bare math layout is built around one hero equation. For side-by-side display, use `math compare`. For a derivation chain, use `math derivation`. Stacking two `$$` blocks in the base layout breaks the visual contract."
    },
    {
      "title": "Symbols without a legend",
      "body": "An equation with three undefined symbols is a puzzle, not a claim. Either every non-trivial symbol gets a legend entry, or the equation is simple enough that the audience knows it cold."
    },
    {
      "title": "ASCII math instead of KaTeX",
      "body": "Writing `beta_hat = (X'X)^-1 X'y` as plain text bypasses the renderer. Always wrap math in `$$…$$` (display) or `$…$` (inline) — KaTeX is the entire reason this layout exists."
    }
  ],
  "related": [
    {
      "name": "code",
      "when": "the implementation, not the equation, is the argument"
    },
    {
      "name": "diagram",
      "when": "the structure of the model, not its closed form"
    },
    {
      "name": "stats",
      "when": "a row of statistical results without a single equation focus"
    },
    {
      "name": "content",
      "when": "one inline equation inside a paragraph of prose"
    }
  ],
  "variantDocs": {
    "feature": {
      "label": "feature",
      "summary": "Alias for the base layout — eyebrow, headline, hero equation, legend.…",
      "sample": "<!-- _class: math feature -->\n\n`Logistic regression · MLE`\n\n## feature crowns the equation full-canvas.\n\n$$ \\ell(\\beta) = \\sum_{i=1}^{n} \\left[ y_i \\log \\sigma(x_i^\\top \\beta) + (1 - y_i) \\log\\bigl(1 - \\sigma(x_i^\\top \\beta)\\bigr) \\right] $$\n\n- $\\ell$ — log-likelihood, concave in $\\beta$\n- $\\sigma$ — logistic link, $\\sigma(z) = 1/(1+e^{-z})$\n- $y_i$ — observed label, $\\in \\{0,1\\}$\n- $x_i$ — feature vector for observation $i$\n"
    },
    "derivation": {
      "label": "derivation",
      "summary": "Two-column table: derivation steps on the left, the justification for…",
      "sample": "<!-- _class: math derivation -->\n\n## derivation walks the steps line by line.\n\n| Step                                                     | Justification             |\n| -------------------------------------------------------- | ------------------------- |\n| $f(x+h) = f(x) + f'(x)\\,h + O(h^2)$                      | Taylor expansion, $n = 2$ |\n| $f(x+h) - f(x) = f'(x)\\,h + O(h^2)$                      | subtract $f(x)$           |\n| $\\dfrac{f(x+h)-f(x)}{h} = f'(x) + O(h)$                  | divide by $h \\neq 0$      |\n| $\\displaystyle\\lim_{h\\to 0} \\dfrac{f(x+h)-f(x)}{h} = f'(x)$ | take the limit            |\n"
    },
    "theorem": {
      "label": "theorem",
      "summary": "Stacked color-coded blockquote cards in the formal-statement vocabul…",
      "sample": "<!-- _class: math theorem -->\n\n## theorem boxes the statement and its proof.\n\n> **Definition.** A function $f : [a,b] \\to \\mathbb{R}$ is *continuous* on $[a,b]$ if $\\lim_{x\\to c} f(x) = f(c)$ for every $c \\in [a,b]$.\n\n> **Theorem.** Let $f$ be continuous on $[a,b]$ and let $y$ lie strictly between $f(a)$ and $f(b)$. Then there exists $c \\in (a,b)$ with $f(c) = y$.\n\n> **Proof.** Set $S = \\{x \\in [a,b] : f(x) < y\\}$. $S$ is non-empty and bounded; let $c = \\sup S$. Continuity at $c$ forces $f(c) = y$. $\\square$\n"
    },
    "compare": {
      "label": "compare",
      "summary": "Side-by-side equation comparison. Each column has its own h3 label, d…",
      "sample": "<!-- _class: math compare -->\n\n## compare sets two formulations side by side.\n\n### Frequentist\n\n$$ \\hat\\theta_{\\text{MLE}} = \\arg\\max_\\theta\\, p(y \\mid \\theta) $$\n\nMaximizes the likelihood — no prior. Uncertainty quantified by the sampling distribution of $\\hat\\theta$ across hypothetical repeats.\n\n### Bayesian\n\n$$ \\hat\\theta_{\\text{MAP}} = \\arg\\max_\\theta\\, p(\\theta \\mid y) $$\n\nMaximizes the posterior — conditions on the prior $p(\\theta)$. Uncertainty is the posterior itself, no repeated sampling required.\n"
    },
    "canvas": {
      "label": "canvas",
      "summary": "Pairs a hero equation with a `functionplot` graph (rendered by the fu…",
      "sample": "<!-- _class: math canvas -->\n\n## canvas gives a long derivation the room.\n\n$$ \\sigma(x) = \\dfrac{1}{1 + e^{-x}} $$\n\nMaps $\\mathbb{R} \\to (0,1)$. $S$-shaped, $\\sigma(0) = 0.5$, steepest slope at the origin.\n\n```functionplot\n{\n  \"data\": [\n    { \"fn\": \"1 / (1 + exp(-x))\" },\n    { \"fn\": \"tanh(x)\" }\n  ],\n  \"xAxis\": { \"domain\": [-6, 6], \"label\": \"x\" },\n  \"yAxis\": { \"domain\": [-1.1, 1.1], \"label\": \"f(x)\" },\n  \"grid\": true\n}\n```\n"
    },
    "matrix": {
      "label": "matrix",
      "summary": "Hero matrix with a properties / dimensions / interpretation legend. B…",
      "sample": "<!-- _class: math matrix -->\n\n## matrix typesets the block structures.\n\n$$\nX = \\begin{pmatrix}\n1 & x_{11} & \\cdots & x_{1p} \\\\\n1 & x_{21} & \\cdots & x_{2p} \\\\\n\\vdots & \\vdots & \\ddots & \\vdots \\\\\n1 & x_{n1} & \\cdots & x_{np}\n\\end{pmatrix}\n$$\n\n- **shape** — $n \\times (p+1)$\n- **rows** — observations\n- **cols** — intercept + $p$ features\n- **rank** — full-rank for OLS to have a unique solution\n- **column 0** — all-ones, absorbs the intercept\n"
    },
    "stats": {
      "label": "stats",
      "summary": "Point estimate with uncertainty (CI, $p$-value, $n$) followed by plai…",
      "sample": "<!-- _class: math stats -->\n\n## stats pairs the estimator with its variance.\n\n$$ \\hat\\beta = 0.42 \\pm 0.03 $$\n\n> 95% CI: $[0.36,\\; 0.48]$\n> $p < 0.001 \\quad\\cdot\\quad n = 1{,}204$\n\nFor every additional unit of exposure, the outcome rises by 0.42 SD — roughly an **8%** shift on the baseline. Effect size is the headline; the $p$-value just rules out chance.\n"
    },
    "decompose": {
      "label": "decompose",
      "summary": "A compound of `matrix`: lays a factorisation out as a sequence of mat…",
      "sample": "<!-- _class: math matrix decompose -->\n\n## decompose colors the terms it names.\n\n$$\n\\begin{pmatrix} 2 & 1 \\\\ 4 & 3 \\end{pmatrix}\n=\n\\begin{pmatrix} 1 & 0 \\\\ 2 & 1 \\end{pmatrix}\n\\begin{pmatrix} 2 & 1 \\\\ 0 & 1 \\end{pmatrix}\n$$\n\n- **$A$** — the original matrix being factorized\n- **$L$** — lower-triangular, unit diagonal\n- **$U$** — upper-triangular\n- **use** — solve $Ax = b$ by forward then back substitution\n"
    }
  },
  "commonMistakes": [
    {
      "mistake": "Eyebrow written as plain or bold text instead of inline code.",
      "fix": "The eyebrow matches `p:has(> code:only-child):has(+ h2)` (the shared before-heading rule) — wrap it in backticks and keep it as the section's first child, immediately before the `## heading`; unwrapped it's just a plain paragraph with no eyebrow styling."
    },
    {
      "mistake": "Legend items lead with plain text instead of the KaTeX symbol, e.g. \"beta hat — OLS coefficient\" instead of the symbol wrapped in `$...$`.",
      "fix": "Each legend `li` should lead with the symbol exactly as it appears in the display equation, wrapped in `$...$` (e.g. `$\\hat\\beta$` — OLS coefficient) — plain text doesn't render as math and breaks the visual match between the equation and its legend."
    }
  ],
  "variantDecisionRule": [
    {
      "variant": "default",
      "useWhen": "Same as `feature` (the bare layout defaults to it) — a single hero equation with a \"where:\" legend."
    },
    {
      "variant": "feature",
      "useWhen": "Explicit synonym for the default layout — one closed-form expression, legend to the side."
    },
    {
      "variant": "derivation",
      "useWhen": "The argument is a multi-step chain (2-4 steps), each needing its own justification — a proof or derivation, not a single closed form."
    },
    {
      "variant": "theorem",
      "useWhen": "Formal Definition/Theorem/Proof exposition for a pure-math audience stating and proving a claim."
    },
    {
      "variant": "compare",
      "useWhen": "Two or three competing formulations side by side (e.g. frequentist vs Bayesian estimators) — the contrast between approaches is the point."
    },
    {
      "variant": "canvas",
      "useWhen": "The equation needs a paired visual — a function plot or diagram — typically for ML/quant audiences illustrating a curve or transformation."
    },
    {
      "variant": "matrix",
      "useWhen": "Linear-algebra-heavy content — a matrix or block structure that needs its own properties/dimensions legend alongside it."
    },
    {
      "variant": "stats",
      "useWhen": "The headline is a point estimate with uncertainty (CI, p-value, n) rather than a bare closed-form equation."
    },
    {
      "variant": "decompose",
      "useWhen": "A factorization — a matrix laid out as a sequence of matrices (e.g. LU, SVD) — a compound of `matrix`, not a single hero matrix."
    }
  ],
  "specimenVoice": true
}
