{
  "$schema": "../../schema/frame.schema.json",
  "id": "compare-code",
  "form": "split",
  "kind": "sovereign",
  "exemptFromChrome": true,
  "description": "Side-by-side code comparison Frame — drives its own `> h2` title grid, so the root masthead does not apply (design/forms.md §8; SOVEREIGN_FRAMES). Suppresses the masthead; keeps the footer.",
  "admits": ["sovereign"],
  "cells": ["stage"],
  "suppresses": ["masthead", "masthead-lede", "masthead-bay"]
}
