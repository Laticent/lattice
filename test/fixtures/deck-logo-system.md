---
marp: true
theme: indaco
color-mode: system
logo: ./acme-logo.svg
---

# The receiver decides

`color-mode: system` defers the canvas to the reader's OS, so the mark defers with it —
a media query rather than a pin.
