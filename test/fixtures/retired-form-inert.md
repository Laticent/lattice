---
theme: indaco
form: standard
---

# Inert deck

This deck carries only the INERT retired shapes — `form: standard` and a bare
`form` token. Both were already no-ops against the default, so the render is
byte-identical with or without them and the CLI must stay silent.

---

<!-- _class: content form -->

## A slide with the inert token

Body text.
